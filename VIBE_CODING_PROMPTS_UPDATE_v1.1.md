# Vibe Coding 提示词集 - v1.1 更新说明

> **本文件是对已生成 3 个 prompt 集（PHASE 0/1/2）的统一改进**
> **创建原因**：PROMPT-01 实战反馈（`upload.ts` 修复时，"1-3 行"约束与推荐方案的 try/catch 块冲突）

---

## 🔍 问题回顾

### 用户反馈原文
> 验收写的「1–3 行」和推荐的 try/catch 块本身有冲突；这是实现该方案所需的最小改动，没有重写文件。

### 问题本质
我在 PROMPT-01 的验收条件里写了 "git diff ... 只显示 1-3 行变更"，但推荐方案本身就是 7 行（try/catch 包裹 mkdirSync）。Cursor 当时如果真的"严守" 1-3 行约束，就会被迫删掉 try/catch，**反而引入了 bug**。

这是提示词设计的一个真实瑕疵 —— **行数约束不能与功能性约束冲突**。

---

## 📐 元规则：当推荐方案与行数约束冲突

**v1.1 起，所有提示词的"范围约束"按以下优先级处理**：

| 优先级 | 约束类型 | 处理方式 |
|--------|---------|---------|
| 1 | 功能完整性 / 安全校验 / 错误处理 | **硬约束**，不可妥协 |
| 2 | "禁止做的事"清单（不碰其他文件、不重构） | 硬约束 |
| 3 | "行数 / 文件数"等量化指标 | **软约束**，允许偏差 ±50% |
| 4 | 代码风格（命名、注释位置） | 软约束，与现有风格一致即可 |

### 简单说

> **try/catch、空值检查、错误传播等标准做法的额外行数是合理的；其余膨胀不允许。**

---

## 🛠️ Cursor 输出格式约定（v1.1 起）

当 Cursor 完成提示词任务后，**必须**在最终输出里附一段：

```markdown
## 实施备注

- 实际改动：[实际行数 / 文件数]
- 推荐方案预估：[预估行数 / 文件数]
- 偏差原因：[解释多了什么 / 少了什么]
- 是否属于"标准做法的合理膨胀"：[是 / 否]
- 自我 review：是否触犯任何"禁止事项"：[逐条勾选]
```

这个 5 行小节帮助人类 reviewer 快速判断 Cursor 是否越界，不需要读完整 diff。

---

## 📋 受影响的提示词（建议人工调整）

以下提示词里出现了"X 行 / Y 文件"这类硬约束，**建议手动放宽**：

| 提示词 | 位置 | 原约束 | 建议改为 |
|--------|------|--------|----------|
| PROMPT-01 | 验收 5 | `git diff ... 只显示 1-3 行变更` | `git diff ... 行数与推荐方案伪代码大致一致（7-10 行内）` |
| PROMPT-04 | 验收 2 | `candidate.service.ts 减少 ~120 行` | `candidate.service.ts 减少与移出方法对应的行数（含方法签名、注释）` |
| PROMPT-11 | 验收 3 | `应用的 service 代码无变更` | 保持不变（schema-only 改动） |
| PROMPT-12 | 验收 4 | 不硬约束行数 | ✅ 已合理 |
| PROMPT-13 | 验收 3 | 列了 6 个文件 | 改为"涉及文件列表如上，**数量不增不减**" |
| PROMPT-14 | 验收 5 | 列了 7 个文件 | 同上 |

**最简洁的修复方法**：直接在每个原 prompt 顶部加一段：

```markdown
## v1.1 注释

本提示词中如出现"X 行变更"、"Y 个文件"等量化约束，
允许 ±50% 偏差，前提是偏差原因是标准错误处理（try/catch、
空值检查、错误传播）。其余偏差视为越界。
```

---

## 🎯 v1.1 起的新提示词模板

下次生成新提示词时，建议模板调整：

```markdown
## 推荐方案
（伪代码）

## 范围约束
- 修改文件清单：[只列文件，不强行约束行数]
- 行数约束：**软目标**，标准错误处理导致的偏差允许
- 不允许的改动：[列禁止事项，不要写成"少改点"]

## 验收条件
1. 功能性验收（必过）
2. 安全性验收（必过）
3. 测试验收（必过）
4. 范围验收（不允许触犯禁止事项）
```

---

## ✅ PROMPT-01 实战是否合规

对照 PROMPT-01 的禁止事项清单：

| 禁止项 | 用户实际操作 | 合规 |
|--------|-------------|------|
| 修改 multer limits / fileFilter | 未动 | ✅ |
| 改 router.post('/', ...) 逻辑 | 未动 | ✅ |
| 把 uploadDir 做成类/单例 | 模块级常量 | ✅ |
| 新增 npm 依赖 | 无 | ✅ |
| 改 files.ts / file.service.ts | 只改 upload.ts | ✅ |

**结论**：PROMPT-01 的实战完全合规，没有越界。
**提示词瑕疵**：验收条件中"1-3 行"约束过严，已在本文件中说明。

---

## 📌 给用户的后续建议

1. **提交时直接用下面任一 commit message**（已在前面回复中给出）
2. **继续做 PROMPT-02**（pino 日志 + PII redact）
   - 这个比 PROMPT-01 复杂，建议预留 1-2 小时
   - 提示词中"5 行 console 替换示例"也属于软目标，可能会有偏差
3. **如果团队对 vibe coding 提示词的精度要求高**，可考虑把所有提示词的行数约束统一扫描一遍，过滤到"软目标"备注中

---

> **创建时间**：基于 PROMPT-01 实战反馈
> **影响范围**：PHASE 0 / PHASE 1 / PHASE 2 三个 prompt 集
> **后续行动**：下次生成 PROMPT-13+ 时，按 v1.1 模板生成
