# 服务器部署提示词（LLM-FIX：M2.7 兼容 + AI 设置优化，2026-09-04）

> **用途**：ATS 服务器从 `bccbc0b` 增量升级到 `e8f7d9f`（当前 master）。
> **前置**：上次部署（9-04 增量）在 `bccbc0b`；本次纯代码，无迁移 / 无新 env / 无 nginx 改动。
> **使用方法**：逐条粘贴给服务器上的 AI 编码工具，对照「✅ 检查点」后继续。
> **总原则**：任何一步不符合检查点 → 停止 → 拿回分析。

---

## 使用前必读（人工确认，不粘贴）

1. **先 push 本地**：`git push origin master`（确认 origin/master = e8f7d9f，✅ 已 push）。
2. **本次变更**（bccbc0b → e8f7d9f，3 commit）：
   - **LLM-FIX**：兼容推理模型（MiniMax M2.7）的 thinking 段输出 → `extractJsonFromLlmContent` 工具 + 5 处 JSON 解析替换 + maxTokens 8000 + 前端 axios 超时 10s→60s + AI 设置模型下拉/官方按钮。
   - docs 2 个（不涉代码）。
3. **无迁移**（prisma 未动）；**无新依赖**（xlsx 上次已加）。
4. **部署后需配 M2.7**：AI 设置页 provider=minimax, model=`MiniMax-M2.7`, baseUrl=`https://api.minimax.chat/v1`, apiKey=已有 → 保存激活 → **测试简历解析/大纲生成**（这是本次修复的核心验证）。

---

## 提示词 ⓪ 全局安全规则（第一段粘贴）

```markdown
# 角色与安全规则（全程有效）
1. 【禁止破坏性】DROP/TRUNCATE/DELETE/rm -rf/git clean/docker system prune/volume rm；不加 --force。
2. 【禁改三类】.env、docker-compose.override.yml、server/uploads/ 不许动，git 不得检出覆盖。
3. 【写白名单】backups/ 备份、git pull、docker build/tag/up -d server、前端构建 client/dist。此外先问我。
4. 【失败即停】报错或不符预期立即停，原样报告，不得重试超 2 次，不得 || true。
5. 【原样汇报】输出原样贴出。
6. 【无迁移】migrate status 应显示 up to date（0 pending）；若出现 pending，停下报告。

确认理解后回复"规则已确认"。
```

---

## 提示词 ① 复核（只读）

```markdown
# 任务：部署前复核
cd /home/ubuntu/recruiting-system

1. pwd && git rev-parse HEAD && git log --oneline -1
   （预期 bccbc0b 开头）
2. git status --short
   （预期是上次记录过的一堆 ?? 杂项：backups/、.env.bak.*、docker-compose.override.yml 等——全无害）
3. docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
   （ats_server / ats_postgres / ats_redis 在运行）
4. df -h / | tail -1          # ≥5G
5. free -h | head -2          # available < 500M 报告（构建可能 OOM）
6. docker-compose exec -T server npx prisma migrate status 2>&1 | tail -4
   # 预期 up to date（0 pending；LLM-FIX 无迁移）
7. docker exec ats_postgres psql -U $(grep '^DB_USER=' .env | cut -d= -f2) -d ats_db -c \
   "SELECT count(*) FROM ai_provider_config;"   # 记录当前 AI 配置行数，部署后对比
8. curl -s http://localhost:3001/api/health | head -c 200
全部原样输出。
```

✅ **检查点**：bccbc0b；杂项无害；三容器在；磁盘 OK；migrate up to date；health ok。

---

## 提示词 ② 备份（精简但不可跳）

```markdown
# 任务：备份
cd /home/ubuntu/recruiting-system
mkdir -p backups
TS=$(date +%Y%m%d_%H%M%S)
docker exec ats_postgres pg_dump -U $(grep '^DB_USER=' .env | cut -d= -f2) -Fc ats_db > backups/pre_llmfix_$TS.dump
docker cp backups/pre_llmfix_$TS.dump ats_postgres:/tmp/check.dump
docker exec ats_postgres pg_restore --list /tmp/check.dump | head -10
docker exec ats_postgres rm /tmp/check.dump
docker tag recruiting-system-server:latest recruiting-system-server:backup-llmfix-$TS
git rev-parse HEAD > backups/rollback_llmfix.txt
echo $TS > backups/last_llmfix_ts.txt
```

✅ **检查点**：dump 可读；镜像 backup-llmfix 标签；rollback_llmfix.txt = bccbc0b 开头。

---

## 提示词 ③ 拉取

```markdown
# 任务：拉取最新 master
cd /home/ubuntu/recruiting-system
1. git fetch origin && git pull origin master
   （预期快进 bccbc0b → e8f7d9f；冲突立即停）
2. git log --oneline -3           # 含 e8f7d9f
3. ls server/src/utils/llm-json.ts   # LLM-FIX 新文件在
4. ls -la .env docker-compose.override.yml   # 与复核一致
5. git status --short               # 只剩 ?? 杂项
```

✅ **检查点**：e8f7d9f 在 HEAD；llm-json.ts 在；受保护文件未动。

---

## 提示词 ④ 构建（内存预检）

```markdown
# 任务：构建 server + 前端
cd /home/ubuntu/recruiting-system

0. free -h | head -2
   # available < 500M → docker stop fitness-hub-frontend-1 fitness-hub-backend-1 fitness-hub-backup-1 crm-app-prod（构建完恢复）

1. docker-compose build server
   docker images | grep recruiting-system-server   # 新 latest + backup-llmfix 标签在

2. docker run --rm -v $(pwd)/client:/app -w /app node:20-alpine \
   sh -c "npm config set registry https://registry.npmmirror.com && npm install --no-audit --no-fund && npm run build"
   ls -la client/dist/index.html   # 时间戳为刚才

⚠️ 失败：停，报错误末尾 50 行。
```

✅ **检查点**：新镜像 + backup 标签；dist 新。

---

## 提示词 ⑤ 切换（无迁移，直接 up）

```markdown
# 任务：重建 server + 验证
cd /home/ubuntu/recruiting-system
1. docker-compose up -d server
2. sleep 25 && curl -s http://localhost:3001/api/health
3. curl -s http://localhost/api/health      # 经 nginx
4. docker-compose logs --tail=30 server     # 无 ERROR、无重启循环
5. docker ps --filter name=ats_ --format "table {{.Names}}\t{{.Status}}"
```

✅ **检查点**：health ok（db/redis/bullmq）；无重启循环。

---

## 提示词 ⑥ 冒烟（重点：LLM-FIX 修复验证）

```markdown
# 任务：冒烟（只读 + LLM 实测）
cd /home/ubuntu/recruiting-system

1. curl -s http://localhost/api/health
2. 登录拿 token：
curl -s -X POST http://localhost/api/auth/login -H "Content-Type: application/json" \
  -d '{"email":"<管理邮箱>","password":"<密码>"}' | head -c 120

3. AI 设置接口（验证新前端/接口）：
curl -s -H "Authorization: Bearer <token>" "http://localhost/api/settings/ai-providers" | head -c 300

4. ⚠️ 关键：M2.7 简历解析实测（LLM-FIX 核心）
   在浏览器（或 curl 模拟）上传一个简历 → 观察 bullmq：
   docker-compose logs --tail=50 server | grep -E "Resume parse|llm" | tail -10
   # 修复前：Failed to parse LLM response as JSON（M2.7 thinking 段）
   # 修复后：应成功解析（不再 Failed）

5. 面试大纲生成实测：
   浏览器打开面试详情页 → 生成大纲 → 确认不报"网络错误"
   # 修复前：axios 10s 超时 → "request aborted"；修复后：60s 内正常完成

6. AI 设置页：确认 model 是下拉（含 MiniMax-M2.7 预置）+ 有「获取官方模型」按钮

全部通过报告"冒烟通过"，附各项输出。
```

✅ **检查点**：简历解析不再 Failed（M2.7 兼容生效）；大纲生成不报网络错误（60s 超时生效）；AI 设置下拉正常。

---

## 提示词 ⑦ 回滚预案（仅失败时）

```markdown
# 任务：回滚
先报告失败现象，再动手。
TS=$(cat backups/last_llmfix_ts.txt)
git checkout $(cat backups/rollback_llmfix.txt)
docker tag recruiting-system-server:backup-llmfix-$TS recruiting-system-server:latest
docker-compose up -d server
docker run --rm -v $(pwd)/client:/app -w /app node:20-alpine \
  sh -c "npm config set registry https://registry.npmmirror.com && npm install --no-audit --no-fund && npm run build"
sleep 20 && curl -s http://localhost/api/health
# LLM-FIX 无迁移，代码回滚即完整回滚
```

---

## 部署后收尾（人工）

1. 若 ④ 停了非 ATS 容器 → docker start 恢复
2. **AI 设置页配置 M2.7**：provider=minimax, model=`MiniMax-M2.7`, baseUrl=`https://api.minimax.chat/v1`, apiKey=已有 → 保存 + 激活 → 测试连接
3. **实测**：上传简历（解析成功）+ 生成面试大纲（无网络错误）——这是 LLM-FIX 的核心验证
4. 观察日志 10 分钟

---

## 部署执行记录（完成后填）

| 阶段 | 结果 | 证据 |
|------|------|------|
| ① 复核 | ⏳ | |
| ② 备份 | ⏳ | |
| ③ 拉取 | ⏳ | |
| ④ 构建 | ⏳ | |
| ⑤ 切换 | ⏳ | |
| ⑥ 冒烟（M2.7 解析/大纲）| ⏳ | |