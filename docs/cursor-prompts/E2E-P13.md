# E2E-P13 职位管理 CRUD API 冒烟 执行提示词（staging 模式）

> **staging 模式**：所有产出写到 `<仓库根>\.dsh\skills\slice-cursor-dev\staging\E2E-P13\`。
> **不要直接修改仓库任何文件**。完成后生成 `apply-e2e-p13.ps1`/`.cmd`（幂等、排除自身/README/REPORT）。

## ⚠️ 强约束（最优先阅读）

1. **staging 模式**：所有文件写到 `<仓库根>\.dsh\skills\slice-cursor-dev\staging\E2E-P13\`（保留相对路径）。
2. **本切片只新增 1 个 e2e 文件**：不动后端、不修前端、不改现有 spec。
   - ✱ `e2e/tests/_jobs-crud.spec.ts`：5 个 e2e 用例（admin project，API 层）
   - 其余一行不动
3. **集成测试已覆盖**（`job.test.ts` 18 个用例），本切片只补 e2e API 层冒烟。
4. **e2e auth 方案**：与之前一致 —— 显式从 `.auth/admin.json` 读 `ats_token`。
5. **不跑验收命令**（DSH 审核后人工跑：`cd e2e && npx playwright test --project=admin tests/_jobs-crud.spec.ts`）。
6. 编码红线：新文件 UTF-8 无 BOM、LF、2 空格、单引号、行尾分号、中文注释；ps1 脚本 UTF-8 带 BOM + 尽量 ASCII 注释。
7. headless 无人工确认：先输出实施计划，然后直接动手。

## 1. 任务 ID + 目标

### 1.1 任务 ID
`E2E-P13`

### 1.2 任务目标

1. **e2e API 冒烟**：职位管理 CRUD 完整闭环——创建 → 列表 → 详情 → 更新 → 关闭/复制 → 删除
2. **回归哨兵**：保护 `POST /api/jobs` + `GET/PATCH/DELETE /:id` + `close/duplicate` 链路不回归
3. **权限校验**：删除仅 admin（非 admin 403）

### 1.3 为什么

职位管理是 ATS 核心业务模块（承载招聘需求入口），现有 UI spec 覆盖页面但 API 层无冒烟保护。本切片补齐。

## 2. 上下文（已实读源码）

### 2.1 前端契约（`client/src/api/job.ts`）

- `createJob(data)` line 124：`POST /jobs`
- `closeJob(id)` line 144：`POST /jobs/:id/close`
- `duplicateJob(id)` line 152：`POST /jobs/:id/duplicate`
- `updateJob(id, data)`：`PATCH /jobs/:id`

### 2.2 后端现状（`server/src/routes/jobs.ts`）

```
POST /api/jobs              创建（登录用户）
GET  /api/jobs              列表（分页+筛选）
GET  /api/jobs/:id          详情
PATCH /api/jobs/:id         更新
POST /api/jobs/:id/close    关闭（status → closed）
POST /api/jobs/:id/duplicate  复制（标题+"（副本）"）
DELETE /api/jobs/:id        删除（仅 admin）
GET/PUT /api/jobs/:id/tags  标签
```

### 2.3 Schema（`jobs.ts:31-44`）

```ts
const createJobSchema = z.object({
  title: z.string().min(2).max(200),
  departments: z.array(z.string().max(50)).min(1),
  level: z.string().min(1).max(50),
  skills: z.array(z.string().max(50)).default([]),
  location: z.string().min(1).max(50),
  type: z.string().min(1).max(50),
  description: z.string().min(1).max(5000),
  requirements: z.string().min(1).max(5000),
  status: z.enum([...JOB_STATUS]).optional().default('open'),
  tagIds: z.array(z.string().max(50)).max(20).optional(),
  pipelineTemplateId: z.string().max(50).nullable().optional(),
});
```

### 2.4 现有模式（与 P12 等一致）

- `loadAdminToken()` 从 `.auth/admin.json` 读 ats_token
- `baseURL` 取 `process.env.BASE_URL` 或 `'http://localhost:5174'`

## 3. 必读约束

### 3.1 反直觉点

1. **departments 是数组**：`['研发部']` 等，至少 1 个。
2. **description/requirements 必填**：min(1)。
3. **复制职位**：标题自动加"（副本）"，可验证 `/duplicate` 返回新职位。
4. **关闭职位**：status → `closed`。
5. **删除仅 admin**：非 admin（hr）调用 DELETE → 403。

### 3.2 不需要 mock

本 e2e 是真接口测试（不 mock）。需要：
- 后端在跑（PG + Redis + backend）
- `.auth/admin.json` 存在（admin token）

## 4. 实施任务

### 4.1 ✱ `staging/E2E-P13/e2e/tests/_jobs-crud.spec.ts`

5 个 e2e 用例（admin project）：

| # | 场景 | 期望 |
|---|---|---|
| 1 | admin 创建职位 → 列表出现 → 详情正确 → 更新 → 关闭 → 删除 | 全 2xx + 状态断言 |
| 2 | 复制职位 → 新职位标题含"（副本）" | |
| 3 | title 少于 2 字 → 400 | |
| 4 | departments 空数组 → 400 | |
| 5 | 非 admin 删除职位 → 403 | |

实现骨架：

```ts
import { test, expect } from '@playwright/test';
import fs from 'fs';
import path from 'path';

const AUTH_DIR = path.join(__dirname, '..', '..', '.auth');
const ADMIN_AUTH_FILE = path.join(AUTH_DIR, 'admin.json');

interface StorageState {
  origins: Array<{ origin: string; localStorage: Array<{ name: string; value: string }> }>;
}

function loadAdminToken(): string {
  const raw = fs.readFileSync(ADMIN_AUTH_FILE, 'utf8');
  const state = JSON.parse(raw) as StorageState;
  const entry = state.origins[0]?.localStorage.find((x) => x.name === 'ats_token');
  if (!entry) throw new Error(`admin.json 缺少 ats_token entry`);
  return entry.value;
}

test.describe('职位管理 CRUD @admin', () => {
  test('admin 创建职位 → 列表出现 → 详情 → 更新 → 关闭 → 删除', async ({ baseURL }) => {
    const token = loadAdminToken();
    const apiBase = baseURL ?? 'http://localhost:5174';
    const headers = { 'Content-Type': 'application/json', Authorization: `Bearer ${token}` };

    const suffix = Date.now().toString().slice(-8);
    const payload = {
      title: `E2E职位-${suffix}`,
      departments: ['研发部'],
      level: 'P6',
      location: '上海',
      type: '全职',
      description: 'E2E 职位描述',
      requirements: 'E2E 职位要求',
    };

    // 1. 创建
    const createRes = await fetch(`${apiBase}/api/jobs`, {
      method: 'POST', headers, body: JSON.stringify(payload),
    });
    expect(createRes.status).toBe(201);
    const created = (await createRes.json()).data;
    expect(created.id).toBeTruthy();
    expect(created.status).toBe('open');

    try {
      // 2. 列表出现
      const listRes = await fetch(`${apiBase}/api/jobs?page=1&pageSize=50`, { headers });
      expect(listRes.status).toBe(200);
      const list = ((await listRes.json()).data ?? []) as Array<{ id: string }>;
      expect(list.some((j) => j.id === created.id)).toBe(true);

      // 3. 详情
      const detailRes = await fetch(`${apiBase}/api/jobs/${created.id}`, { headers });
      expect(detailRes.status).toBe(200);
      const detail = (await detailRes.json()).data;
      expect(detail.id).toBe(created.id);
      expect(detail.title).toBe(payload.title);

      // 4. 更新
      const updateRes = await fetch(`${apiBase}/api/jobs/${created.id}`, {
        method: 'PATCH', headers,
        body: JSON.stringify({ level: 'P7' }),
      });
      expect(updateRes.status).toBe(200);
      expect((await updateRes.json()).data.level).toBe('P7');

      // 5. 关闭
      const closeRes = await fetch(`${apiBase}/api/jobs/${created.id}/close`, {
        method: 'POST', headers,
      });
      expect(closeRes.status).toBe(200);
      expect((await closeRes.json()).data.status).toBe('closed');
    } finally {
      // 6. 删除（admin）
      const delRes = await fetch(`${apiBase}/api/jobs/${created.id}`, {
        method: 'DELETE', headers,
      });
      expect(delRes.ok).toBe(true);
    }
  });

  test('复制职位 → 新职位标题含"（副本）"', async ({ baseURL }) => {
    // ... 创建 → duplicate → 断言新标题
  });

  test('title 少于 2 字 → 400', async ({ baseURL }) => {
    // POST /api/jobs 带 title: 'X' → 400
  });

  test('departments 空数组 → 400', async ({ baseURL }) => {
    // POST /api/jobs 带 departments: [] → 400
  });

  test('非 admin 删除职位 → 403', async ({ baseURL }) => {
    // 用 hr 角色 token（.auth/hr.json）调 DELETE /api/jobs/:id → 403
  });
});
```

### 4.2 ✱ apply 脚本 + README + STAGING_REPORT

模板与 P12 相同（`Copy-Item` 覆盖稿，幂等）。

## 5. 关键决策点

### 5.1 为什么选 jobs
- 核心业务模块（招聘需求入口），UI spec 有但 API 冒烟缺失
- 有完整 mutate 链路（create/update/close/duplicate/delete）适合端到端验证

### 5.2 为什么非 admin 删除用 hr token
`authorize('admin')` 在 DELETE 路由上。用 `.auth/hr.json` 验证 403（与其他 spec 的授权测试模式一致）。

### 5.3 为什么列表用 pageSize=50
避免分页默认 10 条可能查不到刚创建的职位。

### 5.4 不做的
- 不改后端 job.service.ts（已有 18 集成用例）
- 不改前端 job.ts
- 不改 prisma schema / migrations
- 不跑验收命令

## 6. 修改文件清单

### 6.1 staging 产出（4 个）

1. ✱ `e2e/tests/_jobs-crud.spec.ts`
2. ✱ `apply-e2e-p13.ps1` / `.cmd`
3. ✱ `README.md` + `STAGING_REPORT.md`

### 6.2 禁止修改

- `client/**`
- `server/**`
- `e2e/tests/_*.spec.ts`（除新增）
- `e2e/tests/helpers.ts`、`e2e/global-setup.ts`、`e2e/playwright.config.ts`

### 6.3 越界检测

- staging 目录只允许 §6.1 路径
- apply 后 `git diff --stat -- client server e2e/helpers.ts e2e/global-setup.ts e2e/playwright.config.ts` 必须 **0 行**

## 7. 验收标准

### 7.1 硬性验收（DSH 审核方）

| 检查 | 通过标准 |
|---|---|
| `cd e2e && npx playwright test --project=admin tests/_jobs-crud.spec.ts` | 5 全过 |
| 越界 | client / server / 既有 e2e 0 行 |

### 7.2 交付报告模板（STAGING_REPORT.md）

1. 完成范围（1 个文件 + apply/说明）
2. e2e 用例清单（5 条）+ 覆盖矩阵
3. 越界自检
4. 已知风险（依赖 e2e 环境 / 删除权限）
5. 回退方式
6. 红线自检确认

按本提示词直接执行（headless 无人工确认）：先输出实施计划，然后动手到 staging/E2E-P13/。**不要写到仓库根！** 最终回复给出完整交付报告。