# E2E-P10 候选人推进阶段 API 冒烟 执行提示词（staging 模式）

> **staging 模式**：所有产出写到 `<仓库根>\.dsh\skills\slice-cursor-dev\staging\E2E-P10\`。
> **不要直接修改仓库任何文件**。完成后生成 `apply-e2e-p10.ps1`/`.cmd`（幂等、排除自身/README/REPORT）。

## ⚠️ 强约束（最优先阅读）

1. **staging 模式**：所有文件写到 `<仓库根>\.dsh\skills\slice-cursor-dev\staging\E2E-P10\`（保留相对路径）。
2. **本切片只新增 1 个 e2e 文件**：不动后端、不修前端、不改现有 spec。
   - ✱ `e2e/tests/_candidate-stage.spec.ts`：4 个 e2e 用例（admin project，API 层）
   - 其余一行不动
3. **集成测试已覆盖**（candidate.test.ts line 274 等 8 个用例 + candidate.service.test.ts 20+ 个用例），本切片只补 e2e API 层冒烟。
4. **e2e auth 方案**：与之前一致 —— 显式从 `.auth/admin.json` 读 `ats_token`，注入 `Authorization` header。
5. **不跑验收命令**（DSH 审核后人工跑：`cd e2e && npx playwright test --project=admin tests/_candidate-stage.spec.ts`）。
6. 编码红线：新文件 UTF-8 无 BOM、LF、2 空格、单引号、行尾分号、中文注释；ps1 脚本 UTF-8 带 BOM + 尽量 ASCII 注释。
7. headless 无人工确认：先输出实施计划，然后直接动手。

## 1. 任务 ID + 目标

### 1.1 任务 ID
`E2E-P10`

### 1.2 任务目标

1. **e2e API 冒烟**：候选人推进阶段完整路径——admin/hr 创建候选人 → 推进阶段 → 状态校验 → 重复推进校验
2. **回归哨兵**：保护 POST `/api/candidates/:id/stage` 链路不回归
3. **跨服务集成**：候选人创建（已有 P3）+ 阶段推进（本次）→ 完整工作流

### 1.3 为什么

候选人推进阶段是 ATS 核心流程。现有 `interview-permission.test.ts` 等覆盖了面试侧，但候选人阶段推进在 e2e API 层无冒烟保护。

## 2. 上下文（已实读源码）

### 2.1 前端契约（`client/src/api/candidate.ts`）

- `advanceStage(id, data)` line 364：`POST /api/candidates/:id/stage`
- 输入：`{ stage: string, status: 'in_progress' | 'passed' | 'rejected', rejectReason?, assigneeId?, note? }`

### 2.2 后端现状（`server/src/routes/candidates.ts` line 326-336）

```ts
router.post(
  '/:id/stage',
  authenticate,
  validate(candidateIdParamSchema, 'params'),
  validate(advanceStageSchema),
  candidateController.advanceStage
);
```

### 2.3 Schema（`candidates.ts:118-128`）

```ts
const advanceStageSchema = z.object({
  stage: z.string().min(1, '阶段不能为空').max(50),
  status: z.enum([...STAGE_STATUS] as [string, ...string[]], {
    errorMap: () => ({ message: '状态必须是：in_progress, passed 或 rejected' }),
  }),
  rejectReason: z.string().max(500).optional(),
  assigneeId: z.string().max(50).optional(),
  note: z.string().max(500).optional(),
});
```

### 2.4 现有模式（与 P2.5/P3/P4 一致）

- `loadAdminToken()` 从 `.auth/admin.json` 读 ats_token
- `baseURL` 取 `process.env.BASE_URL` 或 `'http://localhost:5174'`
- `test.skip()` 优雅处理候选人缺失场景

## 3. 必读约束

### 3.1 反直觉点

1. **stage 不限固定枚举**：合法选项由候选人适用职位的 Pipeline 模板决定（service 层校验）。测试用 `'初筛'` 这种固定合法阶段。
2. **status 必填且必须 in_progress/passed/rejected**：zod 严格校验。
3. **候选人必须存在**：service 先验证候选人存在（service 层）。
4. **重复推进校验**：service 层校验顺序，不能跳过阶段。
5. **rejectReason 仅 rejected 时有意义**：schema 是 optional 但实际语义上 rejected 应该填。

### 3.2 不需要 mock

本 e2e 是真接口测试（不 mock）。需要：
- 后端在跑（PG + Redis + backend）
- `.auth/admin.json` 存在（admin token）
- 候选人真实存在（P3 已能创建）

## 4. 实施任务

### 4.1 ✱ `staging/E2E-P10/e2e/tests/_candidate-stage.spec.ts`

4 个 e2e 用例（admin project）：

| # | 场景 | 期望 |
|---|---|---|
| 1 | admin 创建候选人 → 推进到下一个阶段 → 通过 | 200 + stage record 创建 |
| 2 | 推进阶段 status 非法值 → 400 |  |
| 3 | candidateId 不存在 → 404 |  |
| 4 | stage 空字符串 → 400 |  |

> 候选 1 含依赖：先调 P3 创建候选人 API（用同一 token），再推进。如果候选人已存在则跳过创建。

实现骨架：

```ts
import { test, expect } from '@playwright/test';
import fs from 'fs';
import path from 'path';

const AUTH_DIR = path.join(__dirname, '..', '..', '.auth');
const ADMIN_AUTH_FILE = path.join(AUTH_DIR, 'admin.json');

interface StorageState {
  origins: Array<{ origin: string; localStorage: Array<{ name: string; value: string }> }>;
}

function loadAdminToken(): string {
  const raw = fs.readFileSync(ADMIN_AUTH_FILE, 'utf8');
  const state = JSON.parse(raw) as StorageState;
  const entry = state.origins[0]?.localStorage.find((x) => x.name === 'ats_token');
  if (!entry) throw new Error(`admin.json 缺少 ats_token entry`);
  return entry.value;
}

test.describe('候选人推进阶段 @admin', () => {
  test('admin 创建候选人 → 推进到下一个阶段 → passed', async ({ baseURL }) => {
    const token = loadAdminToken();
    const apiBase = baseURL ?? 'http://localhost:5174';
    const headers = { 'Content-Type': 'application/json', Authorization: `Bearer ${token}` };

    const suffix = Date.now().toString().slice(-8);
    const createPayload = {
      name: `E2E推进-${suffix}`,
      phone: `138${suffix.slice(-8).padStart(8, '0')}`.slice(0, 11),
      email: `e2e-stage-${suffix}@test.local`,
    };
    const createRes = await fetch(`${apiBase}/api/candidates`, {
      method: 'POST', headers, body: JSON.stringify(createPayload),
    });
    test.skip(createRes.status !== 201, '候选人创建失败（可能 e2e 环境无候选人），跳过');
    const created = (await createRes.json()).data;

    try {
      const advanceRes = await fetch(`${apiBase}/api/candidates/${created.id}/stage`, {
        method: 'POST', headers,
        body: JSON.stringify({ stage: '初筛', status: 'passed' }),
      });
      expect(advanceRes.status).toBe(200);
    } finally {
      // 软删清理
      await fetch(`${apiBase}/api/candidates/${created.id}`, { method: 'DELETE', headers });
    }
  });

  test('推进 status 非法值 → 400', async ({ baseURL }) => {
    const token = loadAdminToken();
    const apiBase = baseURL ?? 'http://localhost:5174';
    const fakeId = 'clcand99999999999999999';
    const res = await fetch(`${apiBase}/api/candidates/${fakeId}/stage`, {
      method: 'POST', headers: {
        'Content-Type': 'application/json',
        Authorization: `Bearer ${token}`,
      },
      body: JSON.stringify({ stage: '初筛', status: 'invalid-status' }),
    });
    expect(res.status).toBe(400);
  });

  test('candidateId 不存在 → 404', async ({ baseURL }) => {
    const token = loadAdminToken();
    const apiBase = baseURL ?? 'http://localhost:5174';
    const fakeId = 'cl99999999999999999999';
    const res = await fetch(`${apiBase}/api/candidates/${fakeId}/stage`, {
      method: 'POST', headers: {
        'Content-Type': 'application/json',
        Authorization: `Bearer ${token}`,
      },
      body: JSON.stringify({ stage: '初筛', status: 'passed' }),
    });
    expect(res.status).toBe(404);
  });

  test('stage 空字符串 → 400', async ({ baseURL }) => {
    const token = loadAdminToken();
    const apiBase = baseURL ?? 'http://localhost:5174';
    const fakeId = 'clcand00000000000000001';
    const res = await fetch(`${apiBase}/api/candidates/${fakeId}/stage`, {
      method: 'POST', headers: {
        'Content-Type': 'application/json',
        Authorization: `Bearer ${token}`,
      },
      body: JSON.stringify({ stage: '', status: 'passed' }),
    });
    expect(res.status).toBe(400);
  });
});
```

### 4.2 ✱ apply 脚本 + README + STAGING_REPORT

模板与 P7 相同（`Copy-Item` 覆盖稿，幂等）。

## 5. 关键决策点

### 5.1 为什么 e2e 用例 1 含创建候选人
候选人推进必须有候选人存在。最稳路径：测试内创建（P3 同模式），用 `test.skip()` 优雅退化。

### 5.2 为什么 stage 用固定值 `'初筛'`
service 校验阶段顺序，'初筛' 是七阶段常量中的第一个（候选人创建后默认就在"入库"，推进到"初筛"合法）。

### 5.3 为什么不需要 mock
e2e 是真接口测试。已有 P3 _candidates-crud.spec 作为参考先例，依赖同样的后端 + DB 环境。

### 5.4 不做的
- 不改后端 candidate.service.ts（已有完整单元/集成测试）
- 不改前端 CandidateDetail.vue
- 不改 prisma schema / migrations
- 不跑验收命令

## 6. 修改文件清单

### 6.1 staging 产出（4 个）

1. ✱ `e2e/tests/_candidate-stage.spec.ts`
2. ✱ `apply-e2e-p10.ps1` / `.cmd`
3. ✱ `README.md` + `STAGING_REPORT.md`

### 6.2 禁止修改

- `client/**`
- `server/**`
- `e2e/tests/_*.spec.ts`（除新增）
- `e2e/tests/helpers.ts`、`e2e/global-setup.ts`、`e2e/playwright.config.ts`

### 6.3 越界检测

- staging 目录只允许 §6.1 路径
- apply 后 `git diff --stat -- client server e2e/helpers.ts e2e/global-setup.ts e2e/playwright.config.ts` 必须 **0 行**

## 7. 验收标准

### 7.1 硬性验收（DSH 审核方）

| 检查 | 通过标准 |
|---|---|
| `cd e2e && npx playwright test --project=admin tests/_candidate-stage.spec.ts` | 4 全过 |
| 越界 | client / server / 既有 e2e 0 行 |

### 7.2 交付报告模板（STAGING_REPORT.md）

1. 完成范围（1 个文件 + apply/说明）
2. e2e 用例清单（4 条）+ 覆盖矩阵
3. 越界自检
4. 已知风险（依赖 e2e 环境 / 候选人推进顺序）
5. 回退方式
6. 红线自检确认

按本提示词直接执行（headless 无人工确认）：先输出实施计划，然后动手到 staging/E2E-P10/。**不要写到仓库根！** 最终回复给出完整交付报告。
