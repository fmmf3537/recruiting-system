# E2E-P11 面试安排 / 取消 API 冒烟 执行提示词（staging 模式）

> **staging 模式**：所有产出写到 `<仓库根>\.dsh\skills\slice-cursor-dev\staging\E2E-P11\`。
> **不要直接修改仓库任何文件**。完成后生成 `apply-e2e-p11.ps1`/`.cmd`（幂等、排除自身/README/REPORT）。

## ⚠️ 强约束（最优先阅读）

1. **staging 模式**：所有文件写到 `<仓库根>\.dsh\skills\slice-cursor-dev\staging\E2E-P11\`（保留相对路径）。
2. **本切片只新增 1 个 e2e 文件**：不动后端、不修前端、不改现有 spec。
   - ✱ `e2e/tests/_interview-schedule.spec.ts`：4 个 e2e 用例（admin project，API 层）
   - 其余一行不动
3. **集成测试已覆盖**（`interview-permission.test.ts` cancel/complete 14 个用例 + `interview-outline.test.ts`），本切片只补 e2e API 层冒烟。
4. **e2e auth 方案**：与之前一致 —— 显式从 `.auth/admin.json` 读 `ats_token`。
5. **不跑验收命令**（DSH 审核后人工跑：`cd e2e && npx playwright test --project=admin tests/_interview-schedule.spec.ts`）。
6. 编码红线：新文件 UTF-8 无 BOM、LF、2 空格、单引号、行尾分号、中文注释；ps1 脚本 UTF-8 带 BOM + 尽量 ASCII 注释。
7. headless 无人工确认：先输出实施计划，然后直接动手。

## 1. 任务 ID + 目标

### 1.1 任务 ID
`E2E-P11`

### 1.2 任务目标

1. **e2e API 冒烟**：面试安排/取消完整路径——admin 创建候选人 → 创建面试 → 取消面试 → 不存在 → 面试官空
2. **回归哨兵**：保护 POST `/api/interviews` + `/api/interviews/:id/cancel` 链路不回归
3. **跨服务集成**：候选人创建（P3/P10）+ 面试创建/取消（本次）

### 1.3 为什么

面试安排是 ATS 核心流程。现有 `interview-permission.test.ts` 覆盖 cancel/complete 权限，但 e2e API 层无冒烟保护。

## 2. 上下文（已实读源码）

### 2.1 前端契约（`client/src/api/interview.ts`）

- `createInterview(data)` line 106：`POST /interviews`
- `cancelInterview(id, reason)` line 121：`POST /interviews/:id/cancel`
- `completeInterview(id)` line 126：`POST /interviews/:id/complete`

### 2.2 后端现状（`server/src/routes/interviews.ts` line 110-115, 220-227`）

```ts
router.post('/', authenticate, validate(createInterviewSchema), interviewController.createInterview);
router.post('/:id/cancel', authenticate, requireRole('admin', 'hr'), validate(interviewIdSchema, 'params'), interviewController.cancelInterview);
```

### 2.3 Schema（`interviews.ts:14-39`）

```ts
const createInterviewSchema = z.object({
  candidateId: z.string().cuid(),
  jobId: z.string().cuid().optional(),
  round: z.enum([...INTERVIEW_ROUNDS]),  // 初试/复试/终面
  type: z.enum([...INTERVIEW_TYPES]),    // 电话/视频/现场
  interviewers: z.array(z.object({ id, name })).min(1),
  scheduledAt: z.string().refine((val) => !isNaN(Date.parse(val))),
  duration: z.number().int().min(15).max(480).optional().default(60),
  location: z.string().optional(),
  notes: z.string().optional(),
  focusType: z.string().optional(),
});
```

### 2.4 现有模式（与 P2.5/P3/P10 一致）

- `loadAdminToken()` 从 `.auth/admin.json` 读 ats_token
- `baseURL` 取 `process.env.BASE_URL` 或 `'http://localhost:5174'`
- `test.skip()` 优雅处理候选人缺失场景

## 3. 必读约束

### 3.1 反直觉点

1. **round/type 是固定中文 enum**：`INTERVIEW_ROUNDS = ['初试', '复试', '终面']`，`INTERVIEW_TYPES = ['电话', '视频', '现场']`。
2. **interviewers 必填**：至少 1 个面试官，每个有 id+name。
3. **scheduledAt 必填**：ISO 时间字符串。
4. **candidateId 必须存在**：service 验证候选人存在。
5. **面试创建会触发**：通知候选人创建人 + 每位面试官 + 自动生成 evaluation 记录（`createPendingEvaluations`）。

### 3.2 不需要 mock

本 e2e 是真接口测试（不 mock）。需要：
- 后端在跑（PG + Redis + backend）
- `.auth/admin.json` 存在（admin token）
- 候选人真实存在（P10 同模式）

## 4. 实施任务

### 4.1 ✱ `staging/E2E-P11/e2e/tests/_interview-schedule.spec.ts`

4 个 e2e 用例（admin project）：

| # | 场景 | 期望 |
|---|---|---|
| 1 | admin 创建候选人 → 创建面试 → 取消面试 | 全 2xx |
| 2 | 取消面试：面试 ID 不存在 | 404 |
| 3 | 创建面试：interviewers 空数组 | 400 |
| 4 | 创建面试：candidateId 不存在 | 404 |

实现骨架：

```ts
import { test, expect } from '@playwright/test';
import fs from 'fs';
import path from 'path';

const AUTH_DIR = path.join(__dirname, '..', '..', '.auth');
const ADMIN_AUTH_FILE = path.join(AUTH_DIR, 'admin.json');

interface StorageState {
  origins: Array<{ origin: string; localStorage: Array<{ name: string; value: string }> }>;
}

function loadAdminToken(): string {
  const raw = fs.readFileSync(ADMIN_AUTH_FILE, 'utf8');
  const state = JSON.parse(raw) as StorageState;
  const entry = state.origins[0]?.localStorage.find((x) => x.name === 'ats_token');
  if (!entry) throw new Error(`admin.json 缺少 ats_token entry`);
  return entry.value;
}

test.describe('面试安排/取消 @admin', () => {
  test('admin 创建候选人 → 创建面试 → 取消面试', async ({ baseURL }) => {
    const token = loadAdminToken();
    const apiBase = baseURL ?? 'http://localhost:5174';
    const headers = { 'Content-Type': 'application/json', Authorization: `Bearer ${token}` };

    const suffix = Date.now().toString().slice(-8);
    // 1. 创建候选人
    const candRes = await fetch(`${apiBase}/api/candidates`, {
      method: 'POST', headers,
      body: JSON.stringify({
        name: `E2E面试-${suffix}`,
        phone: `138${suffix.slice(-8).padStart(8, '0')}`.slice(0, 11),
        email: `e2e-interview-${suffix}@test.local`,
      }),
    });
    test.skip(candRes.status !== 201, '候选人创建失败（e2e 环境可能无候选人），跳过');
    const candidate = (await candRes.json()).data;

    let interviewId: string | undefined;
    try {
      // 2. 创建面试（用 admin 用户自己当面试官）
      const futureTime = new Date(Date.now() + 86400 * 1000).toISOString();
      const interviewRes = await fetch(`${apiBase}/api/interviews`, {
        method: 'POST', headers,
        body: JSON.stringify({
          candidateId: candidate.id,
          round: '初试',
          type: '视频',
          interviewers: [{ id: 'cltvlgcmf0000st38ncxpsrhg', name: '管理员测试' }],
          scheduledAt: futureTime,
          duration: 60,
        }),
      });
      expect(interviewRes.status).toBe(201);
      const interview = (await interviewRes.json()).data;
      interviewId = interview.id;

      // 3. 取消面试
      const cancelRes = await fetch(`${apiBase}/api/interviews/${interviewId}/cancel`, {
        method: 'POST', headers,
        body: JSON.stringify({ reason: 'E2E 测试取消' }),
      });
      expect(cancelRes.status).toBe(200);
    } finally {
      // 清理候选人
      await fetch(`${apiBase}/api/candidates/${candidate.id}`, { method: 'DELETE', headers });
    }
  });

  test('取消面试 ID 不存在 → 404', async ({ baseURL }) => {
    const token = loadAdminToken();
    const apiBase = baseURL ?? 'http://localhost:5174';
    const fakeId = 'cl' + 'x'.repeat(24);
    const res = await fetch(`${apiBase}/api/interviews/${fakeId}/cancel`, {
      method: 'POST', headers: {
        'Content-Type': 'application/json',
        Authorization: `Bearer ${token}`,
      },
      body: JSON.stringify({ reason: '测试' }),
    });
    expect(res.status).toBe(404);
  });

  test('interviewers 空数组 → 400', async ({ baseURL }) => {
    const token = loadAdminToken();
    const apiBase = baseURL ?? 'http://localhost:5174';
    const fakeCandId = 'clcand00000000000000001';
    const futureTime = new Date(Date.now() + 86400 * 1000).toISOString();
    const res = await fetch(`${apiBase}/api/interviews`, {
      method: 'POST', headers: {
        'Content-Type': 'application/json',
        Authorization: `Bearer ${token}`,
      },
      body: JSON.stringify({
        candidateId: fakeCandId,
        round: '初试',
        type: '视频',
        interviewers: [],  // 空数组
        scheduledAt: futureTime,
        duration: 60,
      }),
    });
    expect(res.status).toBe(400);
  });

  test('candidateId 不存在 → 404', async ({ baseURL }) => {
    const token = loadAdminToken();
    const apiBase = baseURL ?? 'http://localhost:5174';
    const fakeCandId = 'cl99999999999999999999';
    const futureTime = new Date(Date.now() + 86400 * 1000).toISOString();
    const res = await fetch(`${apiBase}/api/interviews`, {
      method: 'POST', headers: {
        'Content-Type': 'application/json',
        Authorization: `Bearer ${token}`,
      },
      body: JSON.stringify({
        candidateId: fakeCandId,
        round: '初试',
        type: '视频',
        interviewers: [{ id: 'cltvlgcmf0000st38ncxpsrhg', name: '管理员测试' }],
        scheduledAt: futureTime,
        duration: 60,
      }),
    });
    expect(res.status).toBe(404);
  });
});
```

### 4.2 ✱ apply 脚本 + README + STAGING_REPORT

模板与 P10 相同（`Copy-Item` 覆盖稿，幂等）。

## 5. 关键决策点

### 5.1 为什么用 admin 用户自己当面试官
e2e 环境无稳定 interview 用户，admin 自己的 id 是确定的（来自 `.auth/admin.json`）。production 用 `interviewer` 测试用户，但 e2e 简化。

### 5.2 为什么 scheduledAt 用未来时间
service 校验面试时间不能太早（避免历史面试）。用 `Date.now() + 86400000`（明天）。

### 5.3 为什么不需要 mock
e2e 是真接口测试。已有 P3/P10 创建立候选人 API 模式，依赖同样后端 + DB 环境。

### 5.4 不做的
- 不改后端 interview-scheduler.service.ts（已有完整单元/集成测试）
- 不改前端 interview.vue
- 不改 prisma schema / migrations
- 不跑验收命令

## 6. 修改文件清单

### 6.1 staging 产出（4 个）

1. ✱ `e2e/tests/_interview-schedule.spec.ts`
2. ✱ `apply-e2e-p11.ps1` / `.cmd`
3. ✱ `README.md` + `STAGING_REPORT.md`

### 6.2 禁止修改

- `client/**`
- `server/**`
- `e2e/tests/_*.spec.ts`（除新增）
- `e2e/tests/helpers.ts`、`e2e/global-setup.ts`、`e2e/playwright.config.ts`

### 6.3 越界检测

- staging 目录只允许 §6.1 路径
- apply 后 `git diff --stat -- client server e2e/helpers.ts e2e/global-setup.ts e2e/playwright.config.ts` 必须 **0 行**

## 7. 验收标准

### 7.1 硬性验收（DSH 审核方）

| 检查 | 通过标准 |
|---|---|
| `cd e2e && npx playwright test --project=admin tests/_interview-schedule.spec.ts` | 4 全过 |
| 越界 | client / server / 既有 e2e 0 行 |

### 7.2 交付报告模板（STAGING_REPORT.md）

1. 完成范围（1 个文件 + apply/说明）
2. e2e 用例清单（4 条）+ 覆盖矩阵
3. 越界自检
4. 已知风险（依赖 e2e 环境 / 面试时间校验）
5. 回退方式
6. 红线自检确认

按本提示词直接执行（headless 无人工确认）：先输出实施计划，然后动手到 staging/E2E-P11/。**不要写到仓库根！** 最终回复给出完整交付报告。
