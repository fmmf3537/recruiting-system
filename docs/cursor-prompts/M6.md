# M6 收尾·合规文案 + prompt 调优 + 文档 + 部署方案 执行提示词

## ⚠️ 强约束（最优先阅读）

1. 本切片为**混合型**（服务端小改 + 文档），**可触碰**：`server/src/**` 中与合规文案 / LLM prompt 直接相关的文件 + 根目录文档（`DEPLOY.md`、`.env.example`）+ 新增 `PHASE5_SUMMARY.md`。
2. **禁止触碰**：`client/**`、`e2e/**`、`server/prisma/**`（schema / migrations）、任何 package.json / tsconfig / vite / eslint；**禁止新增依赖**（如发现需要新依赖，禁止安装，改为在交付报告「遗留风险」中说明）。
3. **编码红线**：禁整文件重写既有文件；新文件 UTF-8 无 BOM、LF 行尾、2 空格缩进、单引号；中文注释。
4. **不跑验收命令**（`pnpm test` / `build` / `lint` 都不跑，审核方重跑）。
5. **文档改动集中在**：`.env.example` 补齐阶段 5 新增环境变量注释；新建 `PHASE5_SUMMARY.md`（阶段 5 完成总结）；`DEPLOY.md` 如有部署步骤需补充则补（无则不改）。
6. headless 无人工确认：先输出实施计划，然后直接动手。

## 1. 任务 ID + 目标

### 1.1 任务 ID
M6

### 1.2 任务目标
阶段 5 收尾，PRD §8 非功能需求中的：个保法合规（§8.6 简历 AI 处理授权）、LLM prompt 检查（§8.1）、环境变量补齐、阶段 5 总结文档、部署方案核对。**核心是合规文案**——当前候选人授权同意文案未覆盖「简历内容发送给第三方 AI 处理」的用途。

### 1.3 交付物
1. `server/src/services/referral.service.ts`：consentNote 文案**追加 AI 处理用途**（猎头渠道简历也走 AI 打分/解析，需覆盖）。
2. 候选人授权**相关 UI 文案**检查（`server/src/routes/candidates.ts` 建候选人 zod 里 consentNote 说明、`client/` 不碰——如发现候选人生成表单里授权文案需改，**只提示在交付报告中说明，不改 client**）。
3. 4 个 LLM prompt 检查（match-score / jd-assist / interview-outline / resume-parser）——确认 system prompt 已含「只输出 JSON」「结构 N 层校验」「敏感信息脱敏」等基础要求；如缺失，**有条件修改 lib/llm.ts 或对应 service 的 prompt 模板**。
4. `.env.example` 补齐阶段 5 新增环境变量（`HR_SCORE_CRON` 等 4 个 + 确认 AiMatchScore / Agency 相关无新增 env）。
5. 新建 `PHASE5_SUMMARY.md`（阶段 5 完成总结，参照根目录 `PHASE3_SUMMARY.md` / `PHASE4_SUMMARY.md` 格式）。
6. `DEPLOY.md` 核对：nginx `/referral/` 与 `/api/referral/` 是否被现有配置覆盖（应已覆盖，**验证即可，不改**）。

## 2. 上下文

### 2.1 已核实事实（起草人已实读源码，可直接采信）

- **consentNote 现状**：
  - `server/src/services/referral.service.ts:178`：`猎头机构（${link.agency.name}）承诺已获候选人授权` —— **未覆盖 AI 处理**。
  - `server/src/lib/llm.ts:95-115`：`extractResumeInfo(resumeText)` 直接把简历文本发给 LLM（`${resumeText}` 模板）—— 简历解析已走 AI。
  - `match-score.service.ts` / `jd-assist.service.ts` / `interview-outline.service.ts`：简历文本/JD 内容也进 LLM prompt。
  - 结论：**候选人简历内容已实际发送给第三方 LLM**，但授权声明**未说明 AI 处理用途**。PRD §8.6 要求覆盖。
- **PRD §8.6 原文**：「简历内容发送给第三方 LLM，候选人授权同意文案需覆盖『AI 处理』用途（检查现有 consentNote 文案，必要时更新）」
- **LLM prompt 现状**（4 个 AI 功能）：
  - `resume-parser` → `extractResumeInfo`（llm.ts）：已要求「只返回 JSON」，无明确脱敏指令。
  - `match-score`（match-score.service.ts）：打分 prompt，已含「只返回 JSON」。
  - `jd-assist`（jd-assist.service.ts）：polish / draft 两个 prompt，已含「只返回 JSON」+ 结构校验重试。
  - `interview-outline`（interview-outline.service.ts）：大纲生成 prompt，已含结构校验 + 重试。
  - 检查点：**prompt 是否明说「不要泄露手机号/邮箱等敏感信息」**——这是合规加分项。
- **环境变量**：`.env.example` 需补齐阶段 5 新增：
  - `HR_SCORE_CRON` / `HR_SCORE_BUSINESS_WEIGHT` / `HR_SCORE_PROCESS_WEIGHT` / `HR_SCORE_TALENT_OPS_WEEKLY`（F4-S2 新增，`env.ts` 已有定义）
  - 其他已有（HIRING_DIGEST_CRON / INTERVIEWER_REMINDER_CRON 等）确认是否已写进 .env.example；阶段 5 前创建的不在本切片范围。
- **nginx**：`nginx/nginx.conf` 的 `location /`（try_files → index.html）已覆盖 `/referral/` SPA 路由；`location /api/` 转发 `/api/referral/`。**无需改**，验证即可。

## 3. 必读约束

### 3.1 合规文案修改原则
- consentNote 是候选人授权记录的**溯源字段**，改文案**只影响新增候选人**；历史数据不改（历史不回溯原则）。
- 文案要**同时**覆盖：
  1. 已获候选人授权
  2. 简历内容可能发送给第三方 AI 服务处理（诊断/打分/简历解析等）
- 示例文案（可微调）：
  ```
  猎头机构（${name}）承诺已获候选人授权，并知悉简历可能经第三方 AI 服务处理（用于简历解析、人岗匹配等招聘用途）。
  ```

### 3.2 LLM prompt 合规检查点
- **敏感信息指令**：prompt 需包含「不要在输出中回显手机号/邮箱等个人敏感信息，如与原简历无关不要引用」类似指令（在 `extractResumeInfo` / `match-score` / `jd-assist` / `interview-outline` 中，如缺失则补）。
- **JSON 约束**：已满足（「只输出 JSON」），确认即可。
- **fail-safe 已满足**：验证异常重试 1 次 + 结构校验（已在 F1/F2/F3 实现），确认即可不改。

### 3.3 文档格式
- `PHASE5_SUMMARY.md` 参照 `PHASE3_SUMMARY.md`（根目录）的格式：完成时间 / 阶段目标 / 切片完成表（5 个 F-J+ 个切片）/ 功能清单（F1-F5+F4）/ 数据增长（测试数从 542 → 当前）/ 关键技术决策（哈希去重、版本管理、公开路由安全、过程分 fail-safe）/ 验收基线表 / 遗留与已知问题 / 团队上手指南 / 设计参考文档。
- 阶段 5 切片清单（全部已 commit）：S0 / F2-S / F2-C / F1-S / F1-C / F3-S / F3-C / F5-S / F5-C / F4-S1 / F4-S2 / F4-C（12 个切片 ✅）。
- 完成时间：2026-09-03。

## 4. 实施任务

### 4.1 `server/src/services/referral.service.ts`（条件修改）

`createCandidateFromReferral` 函数约 178 行的 consentNote 文案修改为覆盖 AI 处理（见 §3.1 示例）。**仅改这一行**，其他不动。

### 4.2 `server/src/lib/llm.ts`（条件修改，可选）

如 `extractResumeInfo` 的 user prompt 未含敏感信息指令，追加一行（中文注释在前）：
```
注意：只提取简历中的公开信息（姓名/学历/工作经历等招聘相关字段），不要回显手机号、邮箱等联系方式；不要编造信息。
```

### 4.3 `server/src/services/match-score.service.ts`（条件修改，可选）

如打分 prompt 未含敏感信息指令，追加类似指令。**只改 prompt 模板字符串**，不改逻辑。

### 4.4 `server/src/services/jd-assist.service.ts`（条件修改，可选）

如 polish / draft 两 prompt 未含敏感信息指令，追加。**只改 prompt 模板**。

### 4.5 `server/src/services/interview-outline.service.ts`（条件修改，可选）

如大纲生成 prompt 未含敏感信息指令，追加。**只改 prompt 模板**。

### 4.6 `.env.example`（条件修改）

确认以下变量已存在注释 + 默认值示例；缺失则追加（**放文件末尾「阶段 5 新增」分组**）：

```bash
# ---------- 阶段 5：HR 考核积分（F4） ----------
# HR 考核过程的每日 cron（过程分计算 + 日快照），false/空则关闭
HR_SCORE_CRON=0 2 * * *
# 业务分 / 过程分权重（需 sum≈100；默认 70/30）
# HR_SCORE_BUSINESS_WEIGHT=0.7
# HR_SCORE_PROCESS_WEIGHT=0.3
# 人才库维护周目标次数（默认 5）
# HR_SCORE_TALENT_OPS_WEEKLY=5
```

### 4.7 ✱ `PHASE5_SUMMARY.md`（新增，阶段 5 完成总结）

按 §3.3 格式写。含：
- 12 个切片完成表（commit hash 可从 git log 查）
- 功能清单 F1-F5（JD 完善 / 简历打分 / 面试大纲 / 猎头通道 / HR 考核）
- 数据增长（测试 542 → 现在，可用 54 文件 / 561 用例；代码行数）
- 关键设计决策（每个 F 的 1-2 个亮点）
- 验收基线表（从 AGENTS.md 抄）
- 遗留与已知问题（每个 F 的 known issues 汇总）
- 团队上手指南（dev 启动 + 4 角色账号 + F1-F5 试用步骤）
- 设计参考文档（PRD / 12 个切片提示词）

### 4.8 `DEPLOY.md`（核实，不改除非必需）

核实 `/referral/` 与 `/api/referral/` 已被 nginx 现有配置覆盖（`location /` + `location /api/`）。**结论写进交付报告即可，nginx 配置默认不改**。如需补部署步骤（如「迁移 5 个阶段 5 migration」），可追加一段「阶段 5 部署补充」。

## 5. 关键决策点

### 5.1 合规文案只改新增，回溯用文档说明
consentNote 历史数据必须保留（溯源证据链）。改文案只影响新增候选人；对历史候选人，如后续触发 AI 打分/解析，**由 service 层决定是否要求补充授权** —— 本切片**不做强制拦截**（避免误伤存量流程），仅在 PHASE5_SUMMARY 文档说明该合规风险由人工跟进。

### 5.2 LLM prompt 敏感信息指令
统一要求：**prompt 中加入「不回显联系方式等敏感信息」**。落在 4 个 prompt 的 user/system 模板里，不新增独立函数。

### 5.3 部署方案「迁移步骤」到位情况
阶段 5 的 5 个 migration（AiMatchScore / InterviewOutline / Agency / HrScore / user-role-hr）已在 dev 库 apply。生产部署时「5 个迁移依次 deploy」已在部署脚本跑通。本切片确认 DEPLOY.md 有对应说明即可。

## 6. 修改文件清单

### 6.1 必改文件（3 个；✱=新增，其余为条件修改）
1. `server/src/services/referral.service.ts`（consentNote 一行）
2. `.env.example`（阶段 5 env 分组追加）
3. ✱ `PHASE5_SUMMARY.md`（新增总结）

**可能追加**（仅当 prompt 缺敏感信息指令才改）：
4. `server/src/lib/llm.ts`（可选）
5. `server/src/services/match-score.service.ts`（可选）
6. `server/src/services/jd-assist.service.ts`（可选）
7. `server/src/services/interview-outline.service.ts`（可选）

**只核实不改**：`DEPLOY.md`、`nginx/nginx.conf`。

### 6.2 禁止修改文件
- `client/**`、`e2e/**`、`server/prisma/**`、任何 package.json / tsconfig / vite / eslint
- `server/prisma/migrations/**`（阶段 5 迁移已 apply，不动）
- scheme / 数据库操作：**不执行** prisma migrate / generate / seed

### 6.3 越界检测（交付前自检）
- `git status --short` 只允许出现 6.1 的路径。
- `git diff --stat -- client e2e` 必须 0 行。
- `git diff --stat -- server/prisma` 必须 0 行。
- `.env.example` 只追加，不改已有行。

## 7. 验收标准

### 7.1 硬性验收（审核方执行，你不跑）
- `server pnpm build`（tsc）：0 错误（referent 改动是纯字符串，应 0 影响）。
- `server pnpm test`：基线 54 文件 / 561 用例全过（本切片不改逻辑，如只改 prompt 字符串应全过）。
- `server pnpm lint:check`：不新增 error / warning（prompt 字符串是模板字符串，应 0 影响；如引变量到模板注意模板字面量）。
- `git diff --stat -- client e2e server/prisma`：0 行。
- 新文件 PHASE5_SUMMARY.md 无 BOM；中文注释。

### 7.2 交付报告模板（最终回复必须完整包含）
1. 完成范围概述（是否改 4 个 LLM prompt、哪个缺敏感信息指令补了）。
2. 合规文案修改说明（referral consentNote before→after 1 行）。
3. 4 个 LLM prompt 检查结果表（每个：是否已含 JSON 约束 / 是否已含敏感信息指令 / 本次是否改）。
4. `.env.example` 追加内容。
5. PHASE5_SUMMARY.md 结构概览（章节列表）。
6. DEPLOY.md / nginx 核实结论（/referral/ 是否已覆盖）。
7. 越界自检（git status 全文 + 3 个 0 行检查）。
8. 已知问题与遗留风险（含候选人授权合规风险的人工跟进提示）。
9. 红线自检确认。

按本提示词直接执行（headless 环境无人工确认环节）：先输出实施计划，然后动手，最终回复给出完整交付报告。