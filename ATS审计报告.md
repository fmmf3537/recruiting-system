# 招聘管理系统（ATS）功能审计报告

> 审计时间：2026-08-18
> 审计范围：`server/`（Express + Prisma）、`client/`（Vue 3 + Element Plus）、`e2e/`（Playwright）、部署与安全配置
> 对照基准：主流商用 ATS（Moka、北森、Greenhouse、Lever、Workday Recruiting）及真实企业招聘流程

---

## 一、已实现功能盘点

### 1.1 功能全景

| 模块 | 已实现能力 | 完成度评价 |
|---|---|---|
| 认证与成员 | JWT 登录、登录限流（5 分钟 10 次）、管理员创建成员、修改密码、飞书扫码登录/绑定、admin/member 角色 | 基础完整 |
| 职位管理 | CRUD、多部门（Json 数组）、职级/技能/地域/类型（社招/校招/实习）、状态（open/paused/closed）、标签、按部门数据过滤 | 较完整 |
| HC 编制 | 编制申请（draft→submitted→approved/rejected→fulfilled）、审批意见、与职位关联、已招人数跟踪 | 较完整，亮点 |
| 候选人 | CRUD、多渠道来源、创建时重复检测警告、人才库筛选（未关联职位）、批量推进阶段、批量打标签、工作经历、技能标签 | 较完整 |
| 简历处理 | 上传（PDF/Word）、UUID 重命名、BullMQ 异步队列、LLM（DeepSeek）解析提取结构化信息 | 亮点 |
| 流程阶段 | 固定七阶段（入库/初筛/复试/终面/拟录用/Offer/入职）、负责人指派、淘汰原因、并发保护 | 基本可用 |
| 面试管理 | 面试安排（电话/视频/现场）、面试官冲突检测、日历视图、状态管理（含 no_show）、面试反馈（轮次/结论/富文本） | 较完整 |
| Offer | 创建、结果跟踪（pending/accepted/rejected）、入职确认、实际到岗日期 | 基础可用 |
| 入职 | 入职任务清单（材料/IT/行政/培训四类）、支持批量生成标准任务 | 基础可用 |
| 邮件 | 模板（{{变量}}）、SMTP 发送、发送日志、阶段通过自动触发邮件规则 | 亮点 |
| 通知 | 站内通知（阶段推进/面试安排/Offer 状态/入职提醒）、未读管理 | 基础可用 |
| 沟通记录 | 电话/邮件/微信/短信/面谈记录、下次跟进时间 | 基础可用 |
| 标签/字典 | 标签（颜色/分类）、候选人+职位双向打标、字典分类管理 | 完整 |
| 数据统计 | 核心 KPI 看板、近 7 天趋势、工作量、渠道效果、职位维度、招聘漏斗、招聘周期、Excel 导出 | 较完整，亮点 |
| AI 能力 | 简历 LLM 解析、AI 人岗匹配（人才库推荐） | 亮点 |
| 操作审计 | OperationLog 全量落库（操作人/对象/动作/详情） | 有数据无界面 |
| E2E 测试 | 12 个 Playwright spec（auth/jobs/candidates/interviews/offers/hc/stats 等） | 覆盖良好 |

### 1.2 工程质量观察

- **做得好的**：服务层职责清晰；列表查询有意识的批量取数避免 N+1；Redis 缓存；阶段推进有并发保护；Zod 全量入参校验；关键接口有限流；索引覆盖较全。
- **测试缺口**：单测仅覆盖 5 个 service（candidate/job/offer/stats/resume-parser），`interview-scheduler`、`hc-request`、`email`、`automation-rule`、`notification` 等无单测，与 AGENTS.md 中"services 层覆盖率 ≥80%"的要求有差距。

---

## 二、缺失项与问题清单（按优先级）

### P0 — 安全与合规（真实环境上线前必须解决）

| # | 问题 | 说明 |
|---|---|---|
| 1 | **简历文件无访问控制** | Nginx 将 `/uploads/` 直接暴露为静态目录，拿到 URL 即可下载任何简历（姓名、电话、身份证信息等敏感个人信息），无鉴权、无访问审计。建议改为鉴权后服务端转发（`X-Accel-Redirect`）并记录访问日志。 |
| 2 | **无个人信息保护（PIPL）合规能力** | 候选人数据属敏感个人信息，系统缺少：① 收集授权同意记录；② 数据保留期限与超期自动清理（如淘汰 2 年后匿名化）；③ 候选人数据删除/导出请求响应；④ 简历访问审计。当前 OperationLog 不记录"查看简历"类操作。 |
| 3 | **候选人数据无权限隔离** | `User.department` 字段存在且职位查询已按部门过滤，但候选人列表/详情没有任何部门或归属人隔离——任何 member 可查看全量候选人联系方式。真实 HR 系统至少要求"仅可见我负责/我部门"的候选人。 |
| 4 | **JWT 无法吊销** | logout 仅客户端删 token，无刷新令牌、无黑名单/版本号机制；token 泄露后在有效期内（看配置可能很长）持续有效。成员离职后无法立即收回访问权。 |
| 5 | **无密码找回/重置流程** | 只有"登录后改密"，忘记密码只能找管理员手工处理，且未见管理员重置密码接口。密码策略仅要求 6 位，偏弱。 |
| 6 | **性别为必填、存年龄字段** | `Candidate.gender` 必填 + `age` 字段在招聘场景有就业歧视合规风险，建议改为选填并在列表/筛选默认隐藏。 |

### P1 — 核心流程闭环缺失（对照真实招聘流程的硬伤）

| # | 缺失 | 真实场景对照 |
|---|---|---|
| 7 | **Offer 无审批流** | HC 申请有审批，但发 Offer（涉及薪资成本）反而没有任何审批节点。真实流程：HR 发起 → 部门负责人 → HRD/老板逐级审批通过后才能发 Offer。这是商用 ATS 的标配。 |
| 8 | **面试反馈非结构化** | 只有富文本 + pass/reject/pending 结论，且面试官姓名是手填字符串（HR 代填模式）。真实系统有结构化面试评估表（scorecard）：按维度评分（专业能力/沟通/文化匹配）、评估模板按职位配置、面试官本人登录填写、超时未交反馈自动催收。 |
| 9 | **面试安排无日历/会议集成** | 有冲突检测和日历视图，但没有：① 飞书/Outlook 日历同步（已有飞书 OAuth 基础，成本不高）；② 自动发送 .ics 会议邀请邮件；③ 视频面试链接自动生成（腾讯会议/飞书会议 API）。目前面试官和候选人都要靠人肉通知。 |
| 10 | **无自动化提醒调度器** | `CommunicationLog.followUpAt`（跟进提醒）、面试开始前提醒、阶段停留超时（如初筛 >7 天未处理）都有数据基础，但系统只有 resume-parser 一个 BullMQ worker，没有 delayed/scheduled job 去扫描触发这些提醒，提醒链条是断的。 |
| 11 | **流程阶段全局硬编码** | 七阶段写死在前后端。校招通常有"笔试"，实习可能只有"一面"，高管岗位可能多轮。商用 ATS 支持按职位/类型自定义 pipeline。 |
| 12 | **重复候选人只能警告不能合并** | `duplicate-checker` 只检测不处理，重复简历入库后无合并（merge）能力，时间一长人才库数据会越来越脏。 |
| 13 | **入职后流程断档** | OnboardingTask 到"任务完成"即止，无试用期管理、转正提醒、背调环节（`stage` 与 `type` 字段中均无背调）。 |
| 14 | **无 Offer Letter 生成** | 不能从模板生成 Offer 函（PDF），更无电子签/候选人线上确认入口。 |

### P2 — 招聘源头（获客端整体缺失）

| # | 缺失 | 说明 |
|---|---|---|
| 15 | **无候选人投递门户/招聘官网** | 候选人无法自助投递，全靠 HR 手工录入或上传简历解析。标配是一个公开 careers 页（职位列表 + 投递表单 + 授权同意勾选），这也是 PIPL 授权的天然采集点。 |
| 16 | **无招聘渠道对接与追踪** | `source` 是手填字符串，无渠道专属投递链接/二维码（UTM 式追踪），BOSS/智联/猎聘简历无法自动同步入库，渠道效果分析的数据源头依赖人工填写的准确性。 |
| 17 | **内推功能只有半个字段** | `Candidate.referrer` 存在，但无员工内推入口、内推职位分享、内推进度查询、内推奖励规则。内推通常是质量最高的渠道，值得做完整。 |

### P3 — 体验与效率（锦上添花）

| # | 缺失/不足 | 说明 |
|---|---|---|
| 18 | **无看板（Kanban）视图** | 候选人管理只有表格视图。按阶段拖拽卡片是 ATS 最高频的交互（Greenhouse/Lever/Moka 的核心界面），当前推进阶段要点开弹窗逐个操作。 |
| 19 | **批量操作太少** | 仅批量推进阶段、批量打标签。缺批量淘汰、批量发邮件、批量分配负责人、批量导出。 |
| 20 | **搜索能力弱** | 关键词仅匹配姓名/邮箱/手机号。简历解析后全文未入索引，无法做"搜会 GJB9001C 的候选人"这类技能全文检索（可走 PostgreSQL `tsvector` 或简单全文表）。 |
| 21 | **操作日志无查看界面** | OperationLog 数据在落库，但前端无任何入口，管理员无法追溯"谁改了什么"。 |
| 22 | **报表无订阅推送** | 统计做得较全，但缺"每周一自动把招聘周报发到管理层邮箱/飞书"的订阅机制。 |
| 23 | **统计维度补充** | 建议补：招聘达成率（HC filled/headcount）、Offer 接受率趋势、面试官反馈及时率、阶段停留时长分布。 |
| 24 | **设置页路由均为 hidden** | 字典/邮件模板/自动化规则/标签管理四个 admin 页面在菜单中隐藏，只能通过直链访问，管理员发现性差。 |

---

## 三、改进路线图建议

**第一阶段（上线前，安全合规）**：P0 全项 —— 简历访问鉴权、候选人数据权限隔离、JWT 吊销机制、PIPL 授权与数据保留策略、密码重置。

**第二阶段（流程闭环）**：Offer 多级审批（可复用 HCRequest 的审批模式）→ 结构化面试评估表 + 反馈催收 → 自动化提醒调度器（BullMQ delayed jobs：跟进提醒、面试前提醒、阶段超时预警）→ 日历/会议集成。

**第三阶段（获客与数据质量）**：候选人投递门户（顺带解决 PIPL 授权采集）→ 渠道追踪链接 → 内推模块 → 重复候选人合并。

**第四阶段（体验与洞察）**：看板视图 → 批量操作扩充 → 全文检索 → 操作日志页面 → 周报订阅。

---

*本报告基于代码静态审计，未运行动态验证；标注"未见/无"的条目表示在审计范围内未发现对应实现。*
