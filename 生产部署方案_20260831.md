# 招聘管理系统生产部署方案 v2（2026-08-31，按服务器实际环境修订）

> **v2 修订说明**：经服务器实际侦察，生产环境真实形态与原假设不同，本版已按事实修正：
> - 实际运行的是 `docker-compose.yml + docker-compose.override.yml`（override 是**未入 git 的本地文件**，承载端口映射等关键配置），`docker-compose.prod.yml` 从未跑起来过
> - 容器名是 `ats_server` / `ats_postgres` / `ats_redis`（无 `_prod` 后缀）
> - 网关是**宿主机 nginx**（非容器）：root 指向项目 `client/dist`，`/api/` 反代 `127.0.0.1:3001`
> - 数据库名为 `ats_db`（非 recruitment_system），用户见 .env 的 DB_USER
> - 当前部署版本：`d4a3780`；服务器 git 仅 deploy.sh 权限位变化（无害）
>
> **执行方式**：把每个阶段的提示词逐条粘贴给服务器上的 Kimi Code，拿到输出并对照检查点确认后，再发下一条。
> **总原则**：任何一步不符合检查点 → 立即停止 → 把输出拿回来分析 → 必要时执行第 7 阶段回滚。

---

## 本次部署的变更内容

| 变更 | 风险 | 说明 |
|---|---|---|
| 5 个新数据库迁移 | ⚠️ 中 | enum 类型转换（短暂锁表，SQL 自带空值兜底）、性能索引（纯新增）、列名漂移修复（幂等，生产标准库自动跳过）、软删除字段（新增可空列）、RBAC 4 张新表（纯新增） |
| 后端镜像重建 | 低 | 新增 pino / OTel / Prometheus / Sentry 依赖 |
| 前端重建 client/dist | 低 | 宿主机 nginx 直接指向该目录，替换即生效，无需重启 nginx |
| 新增环境变量 | 无 | 全部可选（缺省=关闭），服务器 .env 不用改 |
| uploads 文件 | 无 | 宿主机目录，容器重建不影响 |
| UploadRecord 历史回填 | 低 | 新增 `server/scripts/backfill-upload-records.ts`，迁移后为老简历补建 upload_record 记录（幂等、纯新增行，见第 4 阶段第 5 步）；不回填则老简历下载 404 |

**兼容性**：所有迁移对旧代码向后兼容，可先迁库再换容器；回滚时多数情况只需回滚代码（A 级）。UploadRecord 回填是纯新增数据，回滚代码后旧代码不读该表，无需撤销。

---

## 第 1 阶段：备份（提示词 ①）——不可跳过

```markdown
# 任务：部署前完整备份

进入 /home/ubuntu/recruiting-system，逐一执行：

## 1. 数据库备份（custom 格式）
mkdir -p backups
TS=$(date +%Y%m%d_%H%M%S)
docker exec ats_postgres pg_dump -U $(grep '^DB_USER=' .env | cut -d= -f2) -Fc ats_db > backups/pre_deploy_$TS.dump

## 2. 验证备份可读（必须做！）
docker cp backups/pre_deploy_$TS.dump ats_postgres:/tmp/check.dump
docker exec ats_postgres pg_restore --list /tmp/check.dump | head -15
docker exec ats_postgres rm /tmp/check.dump
# 能列出表清单 = 备份有效；报错则重新备份，不得继续

## 3. 记录迁移前行数（迁移后对比用）
docker exec ats_postgres psql -U $(grep '^DB_USER=' .env | cut -d= -f2) -d ats_db -c \
"SELECT (SELECT count(*) FROM candidate) c, (SELECT count(*) FROM job) j, (SELECT count(*) FROM offer) o, (SELECT count(*) FROM interview) i, (SELECT count(*) FROM \"user\") u;"

## 4. 文件与配置备份（override 未入 git，必须备份！）
tar czf backups/uploads_$TS.tar.gz server/uploads
cp .env backups/env_$TS
cp docker-compose.override.yml backups/compose-override_$TS.yml
cp docker-compose.yml backups/compose_$TS.yml

## 5. 镜像备份标签 + 回滚锚点
docker tag recruiting-system-server:latest recruiting-system-server:backup-$TS
git rev-parse HEAD > backups/rollback_commit.txt   # 应为 d4a3780...

ls -lh backups/ | tail -8
echo $TS > backups/last_backup_ts.txt
把全部输出原样给我。
```

✅ 检查点：dump 可读、4 个备份文件齐全、镜像 backup 标签存在、rollback_commit.txt 内容为 d4a3780 开头。

---

## 第 2 阶段：拉取新代码（提示词 ②）

```markdown
# 任务：拉取新版本代码

cd /home/ubuntu/recruiting-system
1. git fetch origin
2. git stash list  # 确认无 stash 遗留
3. git pull origin master
   （本地唯一改动是 deploy.sh 权限位，不会冲突；若仍报冲突，立即停止报告）
4. git log --oneline -3   # 最新应为 8270243（部署方案文档）或更新
5. ls server/prisma/migrations/ | tail -6
   应含：20260827125700_add_enum_types / 20260827165000_add_performance_indexes /
   20260827233000_fix_legacy_column_names / 20260828010000_add_candidate_soft_delete /
   20260830020000_add_rbac_tables
6. 确认未跟踪文件未被波及：ls -la .env docker-compose.override.yml server/uploads | head -5
7. 确认新代码的 prisma 目录完整：git status --short（应只剩原本的未跟踪文件）
输出给我。
```

✅ 检查点：版本号正确、5 个迁移文件在、.env / override / uploads 未被 git 触碰。

---

## 第 3 阶段：构建（提示词 ③）

```markdown
# 任务：构建后端镜像与前端产物

cd /home/ubuntu/recruiting-system

## 1. 构建后端镜像（用实际生效的 compose 组合，不带 -f 参数即自动包含 override）
docker-compose build server
docker images | grep recruiting-system-server   # 新 latest + 旧 backup 标签都应在
# 失败：用国内镜像兜底重试一次（server/.npmrc 已内置镜像源，通常不需要额外处理）

## 2. 构建前端产物到 client/dist（宿主机 nginx 直接服务此目录）
docker run --rm -v $(pwd)/client:/app -w /app node:20-alpine \
  sh -c "npm config set registry https://registry.npmmirror.com && npm install --no-audit --no-fund && npm run build"
ls -la client/dist/index.html   # 时间戳必须是刚才

⚠️ 任一构建失败：停止，把错误末尾 50 行原样给我，不要重试超过 2 次。
```

✅ 检查点：新镜像生成、backup 标签还在、index.html 时间戳为刚才。

---

## 第 4 阶段：数据库迁移（提示词 ④）——风险最高

```markdown
# 任务：执行数据库迁移（备份已验证有效）

cd /home/ubuntu/recruiting-system

## 1. 迁移前状态
docker-compose exec -T server npx prisma migrate status 2>&1 | tail -5

## 2. 用新镜像跑迁移（run 会复用项目网络，能连到 postgres 服务）
docker-compose run --rm server npx prisma migrate deploy
⚠️ 必须看到 "All migrations have been successfully applied"
⚠️ 失败立即停止，完整错误给我，禁止继续、禁止 || true

## 3. 结构验证
docker exec ats_postgres psql -U $(grep '^DB_USER=' .env | cut -d= -f2) -d ats_db -c \
"SELECT (SELECT count(*) FROM information_schema.tables WHERE table_name IN ('role','permission','role_permission','user_role')) rbac_tables,
        (SELECT count(*) FROM information_schema.columns WHERE table_name='candidate' AND column_name='deletedAt') softdel_col,
        (SELECT count(*) FROM \"user\" WHERE role IS NULL) null_roles;"
# 预期：4 / 1 / 0

## 4. 业务行数对比（与备份阶段记录的数字逐一对照，必须完全一致）
docker exec ats_postgres psql -U $(grep '^DB_USER=' .env | cut -d= -f2) -d ats_db -c \
"SELECT (SELECT count(*) FROM candidate) c, (SELECT count(*) FROM job) j, (SELECT count(*) FROM offer) o, (SELECT count(*) FROM interview) i, (SELECT count(*) FROM \"user\") u;"
输出给我。

## 5. UploadRecord 历史回填（不做的话，所有老简历下载会 404「文件不存在」）
背景：下载接口 GET /api/files/:filename 强制校验 upload_record 记录存在；
早期上传的简历只有磁盘文件没有记录，新代码部署后这些简历会全部下载失败。
本仓库已带一次性回填脚本 server/scripts/backfill-upload-records.ts（幂等，可重复跑）。

# 5.1 先 dry-run 预览（不写库），输出给我确认
docker-compose run --rm server npx tsx scripts/backfill-upload-records.ts --dry-run

# 5.2 确认条数合理后正式执行
docker-compose run --rm server npx tsx scripts/backfill-upload-records.ts
⚠️ 关注输出中的「磁盘缺失」条数：>0 说明有候选人的 resumeUrl 指向了不存在的文件，把清单给我

## 5.3 回填结果验证
docker exec ats_postgres psql -U $(grep '^DB_USER=' .env | cut -d= -f2) -d ats_db -c \
"SELECT count(*) FROM upload_record;"
# 预期：≥ 第 5.2 步报告的「新建」条数
```

✅ 检查点：迁移全成功、验证 4/1/0、行数与迁移前完全一致、UploadRecord 回填完成且无意外「磁盘缺失」。

---

## 第 5 阶段：切换新版本（提示词 ⑤）

```markdown
# 任务：替换后端容器（nginx 是宿主机的，不用动）

cd /home/ubuntu/recruiting-system
1. docker-compose up -d server    # 只重建 server；postgres/redis 保持不动
2. sleep 25 && curl -s http://localhost:3001/api/health
   预期 database/redis/bullmq 全 ok
3. 经宿主机 nginx 验证全链路：curl -s http://localhost/api/health
4. docker-compose logs --tail=40 server   # 确认无 ERROR、无反复重启
5. docker ps --filter name=ats_ --format "table {{.Names}}\t{{.Status}}"
输出给我。
```

✅ 检查点：直连 3001 和经 nginx 80 的 health 都返回 ok，容器无重启循环。

---

## 第 6 阶段：冒烟测试（提示词 ⑥）

```markdown
# 任务：生产冒烟测试（全部只读，禁止任何写操作）

1. curl -s http://localhost/api/health
2. curl -s http://localhost/ | head -c 200   # 新前端 index.html
3. 登录（用生产管理员真实账号）：
   curl -s -X POST http://localhost/api/auth/login -H "Content-Type: application/json" \
     -d '{"email":"<管理员邮箱>","password":"<密码>"}' | head -c 120
4. 带 token 抽查核心接口（应有数据返回）：
   curl -s -H "Authorization: Bearer <token>" "http://localhost/api/candidates?page=1&pageSize=5" | head -c 300
   curl -s -H "Authorization: Bearer <token>" "http://localhost/api/jobs?page=1&pageSize=5" | head -c 300
   curl -s -H "Authorization: Bearer <token>" "http://localhost/api/tags" | head -c 200
5. 简历下载链路：挑一个候选人详情页在浏览器实测下载/预览（这步我会自己在浏览器做）
6. curl -s http://localhost:3001/api/metrics | head -3   # Prometheus 指标端点
全部通过后报告"冒烟通过"，附各项输出。
```

✅ 检查点：登录成功、列表有数据。然后你本人在浏览器完整点一遍：登录 → 候选人列表 → 详情 → 简历预览 → 职位列表。

---

## 第 7 阶段：回滚预案（仅失败时使用，提示词 ⑦）

```markdown
# 任务：回滚

先报告你要执行哪一级再动手。

## A 级：仅代码回滚（首选，新库结构兼容旧代码）
cd /home/ubuntu/recruiting-system
git checkout $(cat backups/rollback_commit.txt)
TS=$(cat backups/last_backup_ts.txt)
docker tag recruiting-system-server:backup-$TS recruiting-system-server:latest
docker-compose up -d server
# 前端回滚：git checkout 后 client/dist 还是新构建的，需要重新构建旧版前端：
docker run --rm -v $(pwd)/client:/app -w /app node:20-alpine \
  sh -c "npm config set registry https://registry.npmmirror.com && npm install --no-audit --no-fund && npm run build"
sleep 20 && curl -s http://localhost/api/health

## B 级：数据库完整恢复（仅迁移破坏数据时）
docker-compose stop server
TS=$(cat backups/last_backup_ts.txt)
docker cp backups/pre_deploy_$TS.dump ats_postgres:/tmp/restore.dump
docker exec ats_postgres pg_restore -U $(grep '^DB_USER=' .env | cut -d= -f2) -d ats_db --clean --if-exists /tmp/restore.dump
docker exec ats_postgres rm /tmp/restore.dump
# 验证行数后执行 A 级，再 docker-compose up -d

回滚后重跑第 6 阶段冒烟测试。
```

---

## 部署后收尾

1. 观察日志 10 分钟：`docker-compose logs -f server`
2. 确认定时任务（匿名化/提醒）按 .env 配置正常输出
3. 备份在服务器保留 7 天以上再清理
4. **遗留改进项**（不阻塞本次部署，建议之后处理）：
   - `docker-compose.override.yml` 和服务器 `.env` 未入 git，建议整理一份模板入库（脱敏），避免配置只存在于服务器
   - `docker-compose.prod.yml` 从未跑起来过（ats_nginx_prod 挂载冲突），要么修复要么删除，避免误导
   - 新功能开关（REMINDER_CRON_ENABLED 等）默认关闭，如需启用提醒功能，在 .env 加 `REMINDER_CRON_ENABLED=true` 后 `docker-compose up -d server`
