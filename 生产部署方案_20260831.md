# 招聘管理系统生产部署方案（2026-08-31）

> **目标**：把当前 master（含 PROMPT-13 软删除、PROMPT-14 RBAC 骨架、可观测性三件套、列名漂移修复）安全部署到远端生产服务器。
> **执行方式**：开发机完成「第 0 阶段」，其余阶段把对应提示词逐条粘贴给服务器上的 Kimi Code 执行。
> **原则**：先备份、可回滚、每步有验证检查点，任何一步失败立即停止并回滚，不带着问题往下走。

---

## 本次部署的变更内容（供服务器端 Kimi Code 理解背景）

| 变更 | 风险等级 | 说明 |
|---|---|---|
| 5 个新数据库迁移 | ⚠️ 中 | enum 类型转换（会短暂锁表）、性能索引（纯新增）、列名漂移修复（幂等，标准库自动跳过）、候选人软删除字段（新增可空列）、RBAC 4 张新表（纯新增） |
| 后端镜像重建 | 低 | 新增 pino 日志、OTel、Prometheus、Sentry 依赖 |
| 前端重新构建 | 低 | 产物为静态文件，nginx 直接挂载 |
| 上传文件 | 无风险 | `server/uploads` 挂载在宿主机，不在容器内，重建不影响 |

**向后兼容性已确认**：所有迁移对旧版本代码兼容（新增列均可空、新表旧代码不读），因此可以「先迁移数据库，再替换容器」，旧版本在新库结构下能正常工作，回滚数据库非必需时也可只回滚代码。

---

## 第 0 阶段：开发机准备（在本地执行，不是服务器）

```bash
# 0.1 确认工作区干净、master 已推送
cd C:\Users\fmmf\kimi\recruiting-system
git status          # 应显示 nothing to commit
git log origin/master..HEAD --oneline   # 应为空（无未推送提交）
```

✅ 检查点：无未提交改动、无未推送提交。若有，先提交并 `git push`。

---

## 第 1 阶段：服务器定位与快照（提示词 ①）

把下面这段粘贴给服务器上的 Kimi Code：

```markdown
# 任务：定位部署目录并记录当前状态快照

背景：这台服务器上用 Docker 部署着一个招聘管理系统（docker-compose 生产环境，
容器名：ats_postgres_prod / ats_redis_prod / ats_server_prod / ats_nginx_prod）。

请完成：
1. 找到项目目录：`docker inspect ats_server_prod --format '{{json .Config.Labels}}'`
   查看 com.docker.compose.project.working_dir，或直接 find / -name "docker-compose.prod.yml" -not -path "*/proc/*" 2>/dev/null
2. 进入项目目录，记录以下信息并完整输出给我：
   - `git log --oneline -3`（当前部署的代码版本）
   - `git status --short`（服务器上不应有本地改动，如有立即报告，不要动）
   - `docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Image}}"`
   - `docker images | grep recruiting`（当前镜像及 ID）
   - `docker exec ats_server_prod npx prisma migrate status 2>&1 | tail -5`（数据库迁移状态）
   - `ls -la server/uploads | head -5` 和 `du -sh server/uploads`（上传文件情况）
   - `curl -s http://localhost/api/health`（当前健康状态）
3. 确认 .env 存在：`ls -la .env`

⚠️ 本步只读，不要修改任何东西。把以上输出原样返回给我。
```

✅ 人工检查点：确认当前版本号、所有容器 Up、迁移状态无 pending（或记录下 pending 列表以便对比）。

---

## 第 2 阶段：备份（提示词 ②）——最关键的一步，不可跳过

```markdown
# 任务：部署前完整备份（数据库 + 上传文件 + 配置 + 镜像）

在项目目录下执行（目录由上一阶段确认），逐一完成：

## 1. 数据库备份（custom 格式，可 pg_restore）
mkdir -p backups
docker exec ats_postgres_prod pg_dump -U $DB_USER -Fc recruitment_system > backups/pre_deploy_$(date +%Y%m%d_%H%M%S).dump
注意：$DB_USER 从 .env 读取（一般是 postgres）；如果 pg_dump 提示密码，改用：
docker exec -e PGPASSWORD=$(grep DB_PASSWORD .env | cut -d= -f2) ats_postgres_prod pg_dump -U postgres -Fc recruitment_system > backups/pre_deploy_xxx.dump

## 2. 验证备份有效性（必须做！备份不可用等于没备份）
- 文件大小 > 0：`ls -lh backups/`
- 内容可读：`pg_restore --list backups/pre_deploy_xxx.dump | head -20` 能列出表清单
  （宿主机没有 pg_restore 就用容器：docker cp 进去或 docker exec ats_postgres_prod pg_restore --list /tmp/xxx）

## 3. 上传文件备份
tar czf backups/uploads_$(date +%Y%m%d_%H%M%S).tar.gz server/uploads
ls -lh backups/  # 确认两个备份文件都在

## 4. 配置备份
cp .env backups/env_backup_$(date +%Y%m%d_%H%M%S)

## 5. 镜像打备份标签（用于代码回滚）
docker tag $(docker-compose -f docker-compose.prod.yml images -q server) recruiting-system-server:backup-$(date +%Y%m%d)
docker images | grep backup

## 6. 记录回滚锚点
git rev-parse HEAD > backups/rollback_commit_$(date +%Y%m%d).txt
cat backups/rollback_commit_*.txt

把 backups 目录清单完整输出给我。
```

✅ 人工检查点：**4 类备份齐全**（.dump + .tar.gz + env + 镜像 tag），且 .dump 文件用 pg_restore --list 验证可读。缺任何一个都不得继续。

---

## 第 3 阶段：拉取新代码（提示词 ③）

```markdown
# 任务：拉取新版本代码

在项目目录执行：
1. `git fetch origin && git status -sb` —— 确认服务器没有本地未提交改动；如果有，立即停止并报告
2. `git pull origin master`
3. 核对版本：`git log --oneline -3` 应显示最新提交为 ff679c0（PROMPT-14 RBAC）或更新
4. 确认新迁移文件就位：`ls server/prisma/migrations/ | tail -6`，应包含：
   20260827125700_add_enum_types
   20260827165000_add_performance_indexes
   20260827233000_fix_legacy_column_names
   20260828010000_add_candidate_soft_delete
   20260830020000_add_rbac_tables
5. 确认 .env 未被 git 改动（.env 在 gitignore 中，正常不会被覆盖，但要验证）：`ls -la .env`

把 git log 和 migrations 列表输出给我。
```

✅ 人工检查点：版本号正确、5 个迁移文件齐全、.env 未被覆盖。

---

## 第 4 阶段：构建产物（提示词 ④）

```markdown
# 任务：构建后端镜像和前端产物

在项目目录执行：

## 1. 构建后端镜像（buildkit 缓存加速，失败立即停止报告，不要重试超过 2 次）
docker-compose -f docker-compose.prod.yml build server
docker images | grep recruiting-system-server | head -3  # 确认新镜像生成，且 backup 标签还在

## 2. 构建前端产物到宿主机 ./client/dist（生产 nginx 直接挂载这个目录）
docker run --rm -v $(pwd)/client:/app -w /app node:20-alpine sh -c "npm install --no-audit --no-fund && npm run build"
ls -la client/dist/index.html client/dist/assets | head -5  # 确认产物生成且时间戳是刚才

## 3. 确认 mobile/dist 存在（nginx 挂载了它；不存在则创建空目录避免挂载报错）
ls -d mobile/dist 2>/dev/null || mkdir -p mobile/dist

⚠️ 如果 npm install 因网络失败，可用国内镜像重试一次：
docker run --rm -v $(pwd)/client:/app -w /app node:20-alpine sh -c "npm config set registry https://registry.npmmirror.com && npm install --no-audit --no-fund && npm run build"
```

✅ 人工检查点：新镜像已构建、client/dist/index.html 时间戳为刚才、backup 镜像标签仍在。

---

## 第 5 阶段：数据库迁移（提示词 ⑤）——风险最高的一步

```markdown
# 任务：执行数据库迁移（前置备份已在上一阶段完成并验证）

⚠️ 背景：本次有 5 个迁移。其中 add_enum_types 会把若干 text 列转为 enum 类型
（SQL 自带空值兜底，对现有数据安全）；fix_legacy_column_names 是幂等迁移，
在标准库上会自动跳过。全部迁移对旧版代码向后兼容。

在项目目录执行：

## 1. 迁移前状态记录
docker-compose -f docker-compose.prod.yml exec -T server npx prisma migrate status 2>&1 | tail -8

## 2. 执行迁移（通过容器内的新代码执行；先 up 一个临时容器跑迁移也可以，但更简单的方式：）
docker-compose -f docker-compose.prod.yml run --rm --no-deps server npx prisma migrate deploy
⚠️ 注意：必须看到 "All migrations have been successfully applied"
⚠️ 绝不允许用 `|| true` 忽略错误；失败立即停止，把完整错误输出给我，不要执行后续步骤

## 3. 迁移后验证
docker-compose -f docker-compose.prod.yml exec -T postgres psql -U postgres -d recruitment_system -c "
SELECT 'role_enum' AS chk, count(*) FROM \"user\" WHERE role IS NULL
UNION ALL SELECT 'rbac_tables', count(*) FROM information_schema.tables WHERE table_name IN ('role','permission','role_permission','user_role')
UNION ALL SELECT 'soft_delete_col', count(*) FROM information_schema.columns WHERE table_name='candidate' AND column_name='deletedAt';"
预期：role_enum=0，rbac_tables=4，soft_delete_col=1

## 4. 业务数据抽查（对比迁移前后行数不变）
docker-compose -f docker-compose.prod.yml exec -T postgres psql -U postgres -d recruitment_system -c "
SELECT (SELECT count(*) FROM candidate) AS candidates, (SELECT count(*) FROM job) AS jobs, (SELECT count(*) FROM offer) AS offers, (SELECT count(*) FROM interview) AS interviews;"

把以上所有输出原样给我。
```

✅ 人工检查点：迁移全部成功、行数与第 1 阶段快照一致（可多不可少——软删除/RBAC 不产生行业务数据）、验证查询结果符合预期。

---

## 第 6 阶段：切换新版本（提示词 ⑥）

```markdown
# 任务：滚动切换到新版本容器

在项目目录执行：
1. 重建并启动服务（server 和 nginx；postgres/redis 不动）：
   docker-compose -f docker-compose.prod.yml up -d server nginx
2. 等待健康检查通过（最多等 90 秒）：
   docker-compose -f docker-compose.prod.yml ps
   sleep 30 && curl -s http://localhost/api/health
3. 预期 health 返回 "status":"ok"，database/redis/bullmq 均为 ok
4. 查看启动日志确认无异常：
   docker-compose -f docker-compose.prod.yml logs --tail=50 server
把 ps 结果、health 返回、日志末尾输出给我。
```

✅ 人工检查点：容器 Up (healthy)、health 三项全 ok、日志无 ERROR。

---

## 第 7 阶段：冒烟测试（提示词 ⑦）

```markdown
# 任务：生产环境冒烟测试

依次验证（把每步结果输出给我）：
1. 健康检查：curl -s http://localhost/api/health
2. 前端页面：curl -s http://localhost/ | head -c 200（应返回 index.html）
3. 登录接口：curl -s -X POST http://localhost/api/auth/login -H "Content-Type: application/json" \
   -d '{"email":"admin@example.com","password":"<生产管理员密码>"}'
   应返回 success:true 和 token
4. 带 token 验证核心接口（候选人列表/职位列表/标签）：
   curl -s -H "Authorization: Bearer <token>" http://localhost/api/candidates?page=1\&pageSize=5 | head -c 300
   curl -s -H "Authorization: Bearer <token>" http://localhost/api/jobs?page=1\&pageSize=5 | head -c 300
5. 文件下载链路（X-Accel-Redirect）：从候选人列表找一个有简历的候选人，测试下载接口返回 200
6. Prometheus 指标：curl -s http://localhost:3001/api/metrics | head -5（容器内或映射端口）

⚠️ 不要做任何写操作（创建/修改/删除）。全部通过后报告"冒烟测试通过"。
```

✅ 人工检查点：登录成功、列表有数据、文件能下载。然后自己在浏览器里真实登录点一遍核心页面。

---

## 第 8 阶段：回滚预案（仅在失败时使用，提示词 ⑧）

```markdown
# 任务：回滚到部署前版本

根据实际情况选择回滚层级，先报告你要执行哪一级再动手：

## A 级：仅代码回滚（数据库迁移保留——新结构兼容旧代码，绝大多数情况用这级）
cd <项目目录>
git checkout $(cat backups/rollback_commit_*.txt)
docker tag recruiting-system-server:backup-<日期> recruiting-system-server:latest
docker-compose -f docker-compose.prod.yml up -d server nginx
curl -s http://localhost/api/health

## B 级：数据库 + 代码完整回滚（仅在迁移破坏了数据时使用）
# 1. 停应用
docker-compose -f docker-compose.prod.yml stop server nginx
# 2. 恢复数据库（custom 格式，--clean 会 DROP 再重建对象）
docker cp backups/pre_deploy_<时间戳>.dump ats_postgres_prod:/tmp/restore.dump
docker exec ats_postgres_prod pg_restore -U postgres -d recruitment_system --clean --if-exists /tmp/restore.dump
# 3. 验证行数后执行 A 级代码回滚
# 4. 重启全部：docker-compose -f docker-compose.prod.yml up -d

回滚完成后做一遍第 7 阶段冒烟测试，并把结果给我。
```

---

## 部署后观察期（部署完成当天）

- 让服务器 Kimi Code 执行：`docker-compose -f docker-compose.prod.yml logs -f server` 观察 10 分钟有无异常
- 留意 Sentry（如已配置 DSN）有无新错误聚合
- 确认定时任务正常（日志中 cron 相关输出）
- 确认无误后，备份文件建议在服务器保留至少 7 天再清理

## 已知注意事项

1. **deploy.sh 不要用**：里面 `prisma migrate deploy || true` 会吞掉迁移失败，本方案已用手动步骤替代
2. **生产 .env 核对**：`X_ACCEL_REDIRECT=true`、`JWT_SECRET` 为强随机串、`CORS_ORIGIN` 为正式域名、`REMINDER_CRON_ENABLED` 按需开启、LLM key（简历解析用）已配置
3. **enum 迁移锁表**：`ALTER COLUMN TYPE` 会短暂锁表（秒级，数据量小无感），但建议仍选低峰期执行第 5 阶段
4. **失败处置总原则**：任何一步不符合检查点 → 停 → 把输出发给开发机这边分析 → 必要时执行第 8 阶段回滚
