# PRD：字典管理——批量导入 / 导出（含导入模板）

> **版本**：v1.0（2026-09-03）
> **状态**：需求已澄清，待开发
> **定位**：小功能增强（Phase 6 热身 / 独立小切片），预计 0.5~1 天
> **配套**：现有字典管理页 `client/src/views/settings/DictionaryPage.vue` + 后端 `dictionaries` 路由

---

## 1. 背景与目标

### 1.1 现状痛点

| 痛点 | 场景 |
|------|------|
| **跨环境同步难** | dev 环境配好的部门/城市/学历等字典，上线生产要**手工逐个重建**，易漏易错 |
| **批量录入效率低** | 一次性新增 20 个部门 / 10 个技能，只能逐个弹窗点「新增」，成本高 |
| **无备份恢复手段** | 误删字典项（如整分类删掉）无法快速恢复 |
| **分类已超硬编码** | 页面 8 个分类 radio 是写死的，阶段 5 又新增 `interview_focus_type` / `hr_score_rule` / `matching_dimension`，未来还会加——手工维护不现实 |

### 1.2 目标

在现有字典管理页（admin）增加：

1. **导出**：将某个分类（或全部）的字典项导出为 Excel（.xlsx），列结构含所有字段
2. **导入**：从 Excel 批量导入字典项（支持新增 + 覆盖更新）
3. **导入模板**：提供带表头 + 说明行的模板下载，用户按模板填数据

### 1.3 非目标

- 不改变现有单条增删改 API / 页面交互
- 不做分类的动态管理（分类仍由 DEFAULT_DICTIONARIES + 代码约定，不引导用户自建分类）
- 不做跨文件格式（仅 Excel .xlsx / .xls，复用 client 已有 `xlsx@0.18.5` 依赖）

---

## 2. 需求详述

### 2.1 导出

**位置**：字典管理页顶部操作栏（「新增字典项」按钮旁）加「导出」按钮。

**行为**：

| 项 | 方案 |
|----|------|
| 导出范围 | **当前选中分类**（radio 选中的分类）|
| 文件格式 | .xlsx（复用 stats 页导出套路：`XLSX.utils.json_to_sheet` + `book_new` + `writeFile`）|
| 列 | `分类(category)` `编码(code)` `名称(name)` `排序(sortOrder)` `状态(enabled)` `备注/分值(description)` |
| 文件名 | `字典_<category>_<yyyy-MM-dd>.xlsx` |
| 权限 | admin（与页面权限一致，页面本身 requireAdmin）|

> 可选增强（v1.1 再议）：导出全部 = 下拉加「全部分类」选项，导出多 sheet（每分类一 sheet）。

### 2.2 导入

**位置**：同顶部操作栏，加「导入」按钮 → 打开弹窗。

**弹窗流程**：
1. **下载模板**：弹窗内提供「下载导入模板」链接
2. **选择文件**：`el-upload`（accept=".xlsx,.xls"，限制 ≤ 5MB，仅 1 个文件）
3. **预览**：前端解析前 20 行预览（复用 xlsx 读 sheet），展示将导入的行
4. **确认导入**：点击「开始导入」→ 调后端 `POST /api/dictionaries/import`
5. **结果**：显示「成功 N 条 / 跳过 M 条 / 失败 K 条」+ 失败明细（行号 + 原因）

### 2.3 导入模板

模板文件 = 导出文件的**空版本**（只有表头 + 2 行示例 + 说明行），列：

| 列 | 必填 | 说明 |
|----|------|------|
| 分类(category) | ✅ | 如 `department`；与当前选中分类不一致时以**文件内分类为准**（支持跨分类导入）|
| 编码(code) | ✅ | 唯一；重复 code 按覆盖规则处理（见 2.4）|
| 名称(name) | ✅ | 显示名称 |
| 排序(sortOrder) | ⬜ | 空则默认 0 |
| 状态(enabled) | ⬜ | `true/false` 或 `启用/禁用`；空默认 true |
| 备注/分值(description) | ⬜ | 文本；`matching_dimension` 等分类用它存权重数字字符串 |

### 2.4 行级导入规则（核心决策）

| 规则 | 方案 |
|------|------|
| **重复 code（同分类）** | **覆盖更新**（upsert：存在则 update，不存在则 create）——导入即同步，最符合"跨环境同步"诉求 |
| **非法行** | **逐行失败跳过**，不整批回滚；汇总错误明细返回前端展示 |
| **空行 / 表头行** | 跳过（不报错）|
| **category 未识别** | 报错该行（"未知分类"），跳过 |
| **code 缺失 / name 缺失** | 报错该行，跳过 |
| **sortOrder 非数字** | 报错该行，跳过 |
| **enabled 解析失败** | 报错该行，跳过（只认 true/false/1/0/启用/禁用）|

**重要安全点**：导入**不触发 `ensureDefaults`**——加一个 `skipEnsureDefaults` 参数或在导入前不调 getDictionaries，避免空分类被内置默认值抢先填充导致冲突。

**事务**：不用全局事务（逐行 upsert + 失败跳过，部分成功可接受）；每行 upsert 独立提交。

### 2.5 权限与安全

- 导入/导出/模板下载均 **admin only**（复用 `authorize('admin')`）
- 文件校验：multer 接收后用 **magic bytes** 验 .xlsx（xlsx 是 zip，检查 PK\x03\x04 头），拒绝伪造扩展名
- **行数上限**：单次导入 ≤ 1000 行（防超大文件拖垮）
- 导入写 **OperationLog**（action: `dictionary_import`，detail 含 { category?, success, skipped, failed }）
- 导入**不覆盖**内置默认字典的 code 之外的任何逻辑——仅写 Dictionary 表

---

## 3. 接口设计

### 3.1 导出（可选走后端或纯前端）

**方案 A（纯前端，推荐）**：复用现有 `GET /api/dictionaries?category=X` 拿数据，前端用 xlsx 生成文件（与 stats 页一致）。**零后端改动。**

**方案 B（后端）**：`GET /api/dictionaries/export?category=X` 返回 xlsx buffer。适合超大字典（>1K 行），但后端要引 xlsx 依赖（server 无）。

> **默认方案 A**：字典通常 <200 行/分类，前端生成即可，不加后端依赖。向后端导入才需要服务端 xlsx 解析（见下）。

### 3.2 导入（必须后端）

```
POST /api/dictionaries/import
Content-Type: multipart/form-data
body: { file: File }   // .xlsx / .xls，≤1000 行，≤5MB
权限: authenticate + authorize('admin')

返回:
{
  success: true,
  data: {
    success: 12,          // 成功导入/更新的行数
    skipped: 3,           // 空行/重复且规则跳过
    failed: 1,            // 失败行数
    errors: [             // 失败明细（≤20 条，多了截断提示）
      { row: 3, reason: '编码不能为空' }
    ]
  }
}
```

### 3.3 模板下载

**方案 A**：前端生成一个模板 xlsx（硬编码表头 + 示例行），纯前端。**零后端改动。**
**方案 B**：`GET /api/dictionaries/template` 返回模板文件。

> **默认方案 A**，与导出一致。

### 3.4 后端文件

| 文件 | 改动 |
|------|------|
| `server/package.json` | **新增依赖 `xlsx@0.18.5`**（与 client 版本一致；仅在导入时用读 sheet）|
| `server/src/routes/dictionaries.ts` | 加 `POST /import`（multer + authorize('admin') + validate）|
| `server/src/controllers/dictionary.controller.ts` | 加 `importDictionaries` |
| `server/src/services/dictionary.service.ts` | 加 `importDictionaries(file)`（xlsx 解析 + 逐行 upsert + 统计）|
| `server/src/middleware/upload.ts`（如有）| 复用文件校验 / 新增 xlsx magic bytes 检查 |

### 3.5 前端文件

| 文件 | 改动 |
|------|------|
| `client/src/api/dictionary.ts` | 加 `importDictionaries(file)` / `downloadTemplate()` / export 工具 |
| `client/src/views/settings/DictionaryPage.vue` | 顶部操作栏加「导出」「导入」按钮 + 导入弹窗（模板下载 + 文件选择 + 预览 + 结果）|
| `client/src/views/settings/DictionaryPage.vue` | 分类 radio 动态化（从后端分类清单渲染，不硬编码 8 个）——**可选增强**，见 §5 |

---

## 4. 关键设计决策（默认可确认）

| # | 决策 | 默认方案 | 理由 |
|---|------|---------|------|
| 1 | 导出方式 | 纯前端（方案 A）| 零后端改动；字典量小 |
| 2 | 模板方式 | 纯前端（方案 A）| 同上 |
| 3 | 重复 code 处理 | **覆盖更新（upsert）** | 跨环境同步的核心诉求 |
| 4 | 失败行处理 | 逐行跳过 + 错误明细 | 部分成功可接受，不整批回滚 |
| 5 | 导入分类来源 | **文件内 category 列** | 支持一次导入多分类；与当前选中无关 |
| 6 | explore ensureDefaults 交互 | 导入**跳过** ensureDefaults | 防空分类被默认值抢先填充 |
| 7 | 后端依赖 | 新增 `xlsx`（与 client 同版本）| 导入解析必须服务端做 |
| 8 | 行数上限 | 1000 行 | 防超大文件 |
| 9 | 文件校验 | magic bytes（PK 头）+ 大小 + 扩展名 | 防止伪造 xlsx |

---

## 5. 可选增强（v1.1 再议，不阻塞本次）

| 增强 | 说明 |
|------|------|
| 分类动态化 | 字典管理页分类 radio 改从后端返回的分类清单渲染（现在 8 个写死，阶段 5 新增 3 个分类没显示）——**这个其实挺重要**，建议本次一起做或列为独立小切片 |
| 导出全部 | 下拉「全部分类」→ 多 sheet 导出 |
| 导入幂等报告 | 返回已更新 vs 新建的明细（upsert 区分 create/update 计数）|

---

## 6. 验收标准

1. admin 打开字典管理页，点「导出」→ 下载当前分类的 .xlsx，用 Excel 打开列齐全（6 列）
2. 点「导入」→ 弹窗有「下载模板」→ 模板表格头 + 示例行正确
3. 按模板填 3 行（1 新增 + 1 重复 code + 1 缺 name）→ 导入 → 结果：成功 2 / 失败 1，错误明细含行号 3「名称不能为空」
4. 重复 code 的行再次导入 → 覆盖更新成功（值变了）
5. 非 admin（hr）打开页面 → 无导入/导出按钮（或请求 403）
6. 上传非 xlsx 文件（改后缀的 txt）→ 拒绝导入，提示文件类型错误
7. 导入后刷新列表 → 新数据出现（含跨分类导入的生效）
8. 服务器测试：`dictionary.service` 导入单测（新 10+ 用例）+ 集成测试（权限 / 文件校验 / 行级错误）

---

## 7. 开发计划（估算）

| 步骤 | 内容 | 估时 |
|------|------|------|
| 1 | server 导入接口（service + route + 测试）| 0.5 天 |
| 2 | client 按钮 + 弹窗 + 模板 + 导出 | 0.5 天 |
| 3 | 分类动态化（可选）| +0.25 天 |
| 合计 | | 1 ~ 1.25 天 |

**依赖**：server 新增 `xlsx` 依赖；client 已有 `xlsx@0.18.5`。

---

## 8. 风险与开放问题

| 风险 | 缓解 |
|------|------|
| ensureDefaults 与导入竞争（分类为空时默认值先入）| 导入跳过 ensureDefaults；需确认 getDictionaries 调用链 |
| xlsx 依赖体积（server 新增）| 仅在 import router 动态 import（`await import('xlsx')`），不影响启动 |
| 引擎兼容（.xls 旧格式）| xlsx 0.18.5 同时支持 .xlsx/.xls；可只收 .xlsx 简化 |
| 分类动态化改动范围 | 若做，需确认分类来源接口（现有 getDictionaries 无分类清单接口，可能要加 `GET /api/dictionaries/categories`）|

---

## 9. 建议的切片划分

```
方案（本次可整体做，或拆 2 切片）：
- DICT-S1（服务端）：xlsx 依赖 + POST /import + service 逐行 upsert + 测试
- DICT-C1（前端）：导出按钮 + 导入弹窗 + 模板 + 分类动态化
```

建议**先做 DICT-S1 再 DICT-C1**（服务端定了导入语义，前端对接），与阶段 5 切片节奏一致，可走 `docs/cursor-prompts/` 流水线。

---

*PRD 完。确认 §4 关键决策（尤其「重复 code 覆盖更新」和「文件内 category」）后即可进入开发。*