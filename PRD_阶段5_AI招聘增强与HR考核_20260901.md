# PRD：AI 招聘增强与 HR 考核（阶段 5）

> **版本**：v1.1（2026-09-01）
> **状态**：需求已澄清，待开发
> **澄清记录**：9 轮问答，关键决策见各章节「已确认决策」
> **修订记录**：v1.1 —— F3 增强「考察方向」维度（HR 面/技术面/综合面等，字典可配，见 5.1/5.3）
> **前置条件**：生产环境允许调用公有云 LLM API（已确认方案 A），沿用 `server/src/lib/llm.ts` 的 provider 切换机制

---

## 1. 背景与目标

现有 ATS 已完成业务闭环（职位 → 候选人 → 面试 → Offer → 入职）和可观测性建设（阶段 0~4）。本阶段引入 4 个 AI 功能，把系统从「流程记录工具」升级为「招聘效能工具」：

| 编号 | 功能 | 核心价值 | 依赖关系 |
|------|------|---------|---------|
| F2 | 简历自动打分 | 简历筛选提效，人岗匹配量化 | **基础设施**，面试问题生成的参考输入 |
| F1 | JD 完善与辅助生成 | JD 质量提升，降低用人部门沟通成本 | 独立 |
| F3 | 面试问题一键生成 | 面试官准备提效，面试质量标准化 | 依赖 F2 的打分结论（可选输入） |
| F4 | HR 考核积分 | 招聘工作量可视化、可激励 | 独立，但规则最复杂 |

**开发顺序（已确认按依赖重排）**：F2 → F1 → F3 → F4。

---

## 2. 现有基础盘点（复用清单）

| 现有能力 | 位置 | 本阶段复用方式 |
|---------|------|---------------|
| 统一 LLM 调用（4 家 provider 可切换，60s 超时，Prometheus 耗时指标） | `server/src/lib/llm.ts` | 4 个功能全部走 `callLLM`，新增 `purpose` 标签区分用量 |
| AI 人岗匹配 | `ai-matcher.service.ts` | F2 在其思路上升级为多维打分，老接口保留兼容 |
| BullMQ 异步队列（简历解析在用） | `server/src/lib/queue.ts` + `workers/` | F2 上传打分走异步队列，不阻塞上传响应 |
| 字典机制（面试评估维度已用） | `dictionaries` 路由 + `evaluation_dimension` | F2 新增 `matching_dimension` 字典；F4 新增 `hr_score_rule` 字典 |
| 权限矩阵（PROMPT-14 + 阶段 3） | `requirePermission` / 角色中间件 | 新功能按角色挂权限点 |
| OperationLog 操作日志 | 全局 | AI 调用、打分、大纲生成全部写日志 |
| 候选人可见性过滤 | `candidate-visibility.service.ts` | F2 打分、F3 大纲继承现有可见性规则 |
| node-cron 定时任务框架 | `server/src/lib/cron.ts` | F4 过程质量分的每日计算 |
| 站内通知 | Notification 表 + dedupeKey | F2 打分完成通知（可选）、F4 周榜通知（可选） |

---

## 3. F2：简历自动打分（第一个开发）

### 3.1 已确认决策

- **触发方式（D 方案）**：上传简历时选定目标职位 → 自动打分；候选人详情页对后续关联的职位可手动补打
- **打分维度（B 方案）**：字典配置 `matching_dimension`（名称 + 权重 + 启停用 + 排序），seed 默认维度，admin 可在设置页调整
- **结果可见范围**：HR、面试官、用人部门（hiring_manager）、admin 均可查看（用户原话「供 HR、面试官、用人部门参考」），沿用候选人可见性过滤

### 3.2 用户故事

- 作为 HR，我上传简历并选择目标职位后，希望系统在 1 分钟内自动给出该候选人相对此职位的多维打分和推荐结论，不用我逐份精读
- 作为面试官，我接到面试通知时，希望看到候选人的匹配分明细，提前了解强弱项
- 作为用人部门，我希望看到推荐到我部门的候选人的分数排序，优先面高分者

### 3.3 功能流程

```
上传简历（选定职位）
  → 简历解析（现有 BullMQ 流程）
  → 解析完成 → 自动触发 AI 打分任务（新 BullMQ job: ai-match-score）
  → LLM 输入：JD 全文 + 简历结构化信息 + 启用的打分维度（含权重）
  → LLM 输出：各维度分数(0-100) + 理由 + 综合推荐分 + 推荐等级
  → 落库 AiMatchScore → 候选人详情页 / 职位候选人列表展示

手动补打：详情页「AI 打分」按钮 → 选职位 → 同上流程（同步等待或异步均可，建议异步 + 通知）
```

### 3.4 打分输出规范

LLM 返回 JSON：

```json
{
  "overallScore": 82,
  "grade": "推荐",
  "summary": "一句话总评（50 字内）",
  "dimensions": [
    { "name": "专业技能匹配", "weight": 40, "score": 85, "reason": "具备 xx 经验…" }
  ],
  "risks": ["风险点1（如频繁跳槽）"],
  "highlights": ["亮点1"]
}
```

- 推荐等级四档：`强烈推荐(≥85) / 推荐(70-84) / 待定(50-69) / 不推荐(<50)`，阈值写死在代码常量
- `overallScore` = Σ(维度分 × 权重)，服务端重算校验，不信任 LLM 直接给的综合分（防算错）

### 3.5 数据模型（新增 1 表 + 1 字典）

```prisma
model AiMatchScore {
  id           String   @id @default(cuid())
  candidateId  String
  jobId        String
  overallScore Int                    // 0-100，服务端按权重重算
  grade        String                 // 强烈推荐/推荐/待定/不推荐
  summary      String?
  dimensions   Json                   // [{name, weight, score, reason}]
  risks        Json?                  // string[]
  highlights   Json?                  // string[]
  stale        Boolean  @default(false) // JD 或简历更新后标记过期
  model        String?                // 实际调用的模型名，便于追溯
  promptVersion String  @default("v1")
  triggeredBy  String                 // auto=上传自动 / manual=手动补打
  createdById  String?                // 触发人（自动则为上传人）
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  candidate Candidate @relation(...)
  job       Job       @relation(...)

  @@unique([candidateId, jobId])     // 同一候选人对同一职位只保留最新分
  @@map("ai_match_score")
}
```

- 字典 `matching_dimension`：`name / weight / enabled / sort`，seed 默认 5 维：
  专业技能匹配 40%、工作经验与年限 25%、学历与院校背景 15%、职业稳定性 10%、加分项（证书/行业背景）10%

### 3.6 关键设计决策（待确认，有默认方案）

| 决策点 | 默认方案 | 说明 |
|--------|---------|------|
| JD 变更后旧分怎么办 | 标记 `stale=true`，详情页提示"JD 已更新，分数可能过期"，提供「重新打分」按钮 | 不自动重算，避免 JD 微调引发全体候选人 token 风暴 |
| 重复打分 | 同 candidate+job 覆盖更新（upsert），不保留历史版本 | MVP 简化；历史版本留作后续增强 |
| 打分失败 | 不阻塞上传流程；详情页显示"打分失败，可重试"；写 OperationLog | LLM 超时不影响主流程 |
| 成本控制 | 打分前查重：简历文本 hash + JD hash 与上次相同则跳过 LLM 直接复用旧分 | 避免重复上传/重复点击烧钱 |
| 模型档位 | 沿用 `LLM_PROVIDER` 配置；打分建议用较强模型（如 deepseek-chat 或 moonshot-v1-32k），简历长文本注意 8k 上下文截断策略 | 简历全文超限时截取：基本信息 + 最近 2 段经历 + 技能（解析结果已是结构化，通常不超） |

### 3.7 API 设计

| 接口 | 说明 | 权限 |
|------|------|------|
| `POST /api/candidates/:id/match-score` | 手动触发打分（body: jobId） | 登录 + 候选人可见 |
| `GET /api/candidates/:id/match-scores` | 候选人全部职位打分列表 | 登录 + 候选人可见 |
| `GET /api/jobs/:id/match-scores` | 职位下候选人打分排序 | 登录 + 职位部门可见 |
| 字典 CRUD | `matching_dimension` 走现有字典接口 | admin |
| 前端 | 候选人详情页「AI 匹配分」卡片（维度雷达图或条形图 + 理由展开）；职位详情候选人列表加分数列与排序 | - |

### 3.8 验收标准

- 上传简历选定职位后 90 秒内出分（异步），失败可重试且不影响候选人创建
- 综合分 = 维度分加权和（服务端校验，LLM 给错自动纠正）
- 维度权重在字典页调整后，新打分立即生效（旧分不变）
- JD 编辑后关联打分自动标记过期
- 相同简历+JD 重复触发不重复调 LLM（hash 去重）
- 单元测试覆盖打分 service（mock LLM），集成测试覆盖 3 个接口

---

## 4. F1：JD 完善与辅助生成（第二个开发）

### 4.1 已确认决策

- **场景一「完善建议」= A 方案**：诊断报告（问题清单 + 每条理由）+ 完整优化稿，HR 可一键采用或再改
- **场景二「从零生成」= A 方案**：结构化表单（职位名称/部门/职级必填 + 用人需求自由描述选填），AI 参考系统内同类型历史 JD 风格生成
- **两个场景的 AI 产出都不直接覆盖原 JD**，生成草稿 → 人工确认后才保存

### 4.2 用户故事

- 作为 HR，我收到用人部门给的粗糙 JD，希望 AI 指出问题并给一份专业优化稿，我确认后直接发布
- 作为用人部门，我说不清要写什么要求，希望填几个关键信息 + 大白话描述，AI 帮我生成一份像样的 JD 初稿

### 4.3 功能流程

```
场景一（完善）：
职位编辑页「AI 完善建议」按钮
  → 输入：当前 JD 全文 + 职位元信息（职级/部门/类型）
  → LLM 输出：{ issues: [{title, detail, severity}], improvedJd: "完整优化稿" }
  → 前端：左侧原 JD、右侧问题清单 + 优化稿对照，「采用优化稿」按钮覆盖到编辑框（不自动保存）

场景二（生成）：
新建职位页「AI 辅助生成」按钮 → 弹窗表单（名称/部门/职级 + 自由描述）
  → 输入：表单字段 + 系统内同 type 最近 3 份 JD（风格参考，截断控制 token）
  → LLM 输出：完整 JD 草稿（含 岗位职责/任职要求/加分项 结构）
  → 前端：草稿预览 + 「填入编辑器」按钮
```

### 4.4 数据与接口

- **不新增表**：建议稿是对话式产出，确认后落到现有 `job.description`，无需持久化建议历史（MVP）
- 接口：
  - `POST /api/jobs/ai-polish`（body: jdText + 职位元信息）→ 诊断 + 优化稿
  - `POST /api/jobs/ai-draft`（body: 表单字段 + 自由描述）→ JD 草稿
- 权限：hr / hiring_manager / admin 可用（interviewer 不可用）
- 两个接口加 rate limit（15 分钟 20 次），防刷 token

### 4.5 验收标准

- 完善建议的问题清单非空且每条有理由；优化稿完整可解析
- 生成草稿包含「岗位职责 / 任职要求」结构；空自由描述也能基于表单字段生成
- AI 产出未经人工点击不会写入 job 记录
- LLM 失败返回友好错误，页面不崩
- service 单测（mock LLM）+ 接口集成测试

---

## 5. F3：面试问题一键生成（第三个开发）

### 5.1 已确认决策

- **入口 = A 方案**：面试详情/安排页「AI 生成面试大纲」按钮，输入自动组装（该场面试的 JD + 候选人简历 + 前几轮面试反馈与评估结论 + F2 匹配分）
- **调整 = A 方案**：指令式整体再生成（面试官输入调整要求）+ 最终稿手动微调；保留版本历史可回退
- 参考答案随大纲生成，默认折叠；大纲保存在面试记录中，面试后填评估时可对照
- **考察方向区分（2026-09-01 增强需求）**：生成时必须选择本场面试的考察方向（HR 面 / 技术面 / 综合面 / 主管面 / 交叉面等），AI 按方向生成差异化大纲。选项走字典 `interview_focus_type` 配置（admin 可增删），不写死

### 5.2 用户故事

- 作为面试官，我打开明天的面试安排，一键生成针对这个候选人、这个岗位、这一轮**考察方向**的面试问题大纲和参考答案，还能按我的要求调整，不用从零准备
- 作为 HR，我希望各轮面试有分工（HR 面重稳定性与薪资、技术面重专业深度、综合面重复盘与抗压），AI 按方向出题且能参考上一轮反馈避免重复提问

### 5.3 考察方向与出题侧重（prompt 内置指引）

| 考察方向（seed 默认项） | 出题侧重 |
|------------------------|---------|
| HR 面 | 求职动机、职业稳定性、薪资期望、文化匹配、离职原因 |
| 技术面 | 专业技能深度、项目技术细节、问题解决思路、行业/体系标准实操（如 GJB9001C） |
| 综合面 | 项目复盘、抗压与应变、跨部门协作、职业规划 |
| 主管面 | 管理思维、目标拆解、团队匹配、价值观 |
| 交叉面 | 协作场景、沟通风格、上下游配合 |

> 上表是各方向的默认出题指引（写进 prompt 模板），字典只配置方向的「名称与启停用」；admin 新增自定义方向时，AI 按方向名称自行把握侧重。

### 5.4 功能流程

```
面试详情页「AI 生成面试大纲」→ 选择考察方向（默认取面试记录上的 focusType，可改）
  → 输入组装：JD 全文 + 简历结构化信息 + 考察方向（含出题侧重指引）
              + 前几轮 InterviewFeedback/InterviewEvaluation 结论
              + AiMatchScore（如有）+ 本轮次信息（第几轮/面试形式）
  → LLM 输出：{ sections: [{theme, questions: [{question, intent, referenceAnswer, followUp?}]}], durationAdvice }
  → 落库 InterviewQuestionOutline v1（记录本版使用的考察方向）→ 展示（参考答案默认折叠）
调整：输入调整指令（可同时改考察方向）→ 整体再生成 → v2（v1 保留可回退）
定稿：面试官手动编辑文本 → 保存到当前版本
面试后：评估填写页可展开大纲对照打分
```

### 5.5 数据模型（新增 1 表 + Interview 加 1 字段 + 1 字典）

```prisma
// Interview 模型新增字段（考察方向，创建/编辑面试时可填，可空兼容存量）
model Interview {
  // ... 现有字段（round=初试/复试/终面 是轮次，type=电话/视频/现场 是形式，均保留）
  focusType   String?   // 考察方向：HR面/技术面/综合面/主管面/交叉面（字典 interview_focus_type）
}

model InterviewQuestionOutline {
  id          String   @id @default(cuid())
  interviewId String
  version     Int
  focusType   String    // 本版大纲使用的考察方向（快照，不随面试记录变更）
  outline     Json      // {sections:[{theme, questions:[{question, intent, referenceAnswer, followUp}]}]}
  adjustNote  String?   // 本版本的调整指令（v1 为 null）
  editedById  String?   // 手动微调人
  createdById String
  createdAt   DateTime @default(now())

  interview Interview @relation(...)

  @@unique([interviewId, version])
  @@map("interview_question_outline")
}
```

- 字典 `interview_focus_type`：`name / enabled / sort`，seed 默认 5 项（HR 面 / 技术面 / 综合面 / 主管面 / 交叉面）
- 接口：
  - `POST /api/interviews/:id/question-outline`（生成/再生成，body: `{ focusType, adjustNote? }`，focusType 必填）
  - `GET /api/interviews/:id/question-outlines`（版本列表）
  - `PATCH /api/interviews/:id/question-outline/:version`（手动微调定稿）
  - 面试创建/编辑接口的 schema 增加可选 `focusType` 字段（Zod 校验字典值）
- 权限：该场面试的面试官 + HR + admin；参考候选人可见性规则
- 生成按钮防重复点击（loading 锁）；同一面试生成上限 10 版（防刷）

### 5.6 验收标准

- 生成必须选择考察方向；不同方向生成的大纲出题侧重明显不同（HR 面 vs 技术面题目集合重叠度低）
- 生成内容明显区分轮次（第二轮能看到对第一轮反馈的承接/深挖）
- 调整指令生效（如"多考察质量体系经验"→ 新版本相关题目占比上升）；再生成时可改考察方向
- 大纲版本记录当时使用的考察方向（快照），面试记录后续改 focusType 不影响已生成版本
- 版本历史可查看、可回退；手动编辑不触发 LLM
- 参考答案默认折叠，可展开
- 面试官只能看到自己有权限的面试的大纲（越权 403）
- 存量无 focusType 的面试记录可正常生成大纲（生成时选择方向即可）
- service 单测 + 集成测试

---

## 6. F4：HR 考核积分（第四个开发，规则最复杂）

### 6.1 已确认决策

- **计分模型 = 积分制**：每个招聘动作固定分值，周期分数 = 积分总和，不做指标达成率
- **双轨制（C 方案）**：业务积分 + 过程质量分（任何时候可挣），默认权重 70% / 30% 可配置
- **考核对象 = role=hr**（hiring_manager / interviewer 不考核；admin 默认不考核，可配置加入）
- **可见范围 = 本人看明细 + 团队排名；admin 看全员完整报表**

### 6.2 业务积分规则（字典 `hr_score_rule`，category=business）

| 动作 | 数据来源 | 默认分值 | 说明 |
|------|---------|---------|------|
| 简历上传并解析成功 | Candidate.createdById | +2 | 候选人创建即记 |
| 推荐到用人部门（推进到「部门筛选」） | StageRecord | +5 | 防刷：同一候选人只记一次 |
| 完成一场面试（面试状态→completed） | Interview.createdById | +10 | 安排且实际完成才记分 |
| 发出 Offer（状态→sent） | Offer | +30 | - |
| 候选人入职（阶段→已入职） | StageRecord | +50 | 最高单价，体现结果导向 |
| Offer 被拒 / 入职后试用期淘汰 | Offer.result / 阶段回退 | -10 / -20 | 负分体现质量约束，可配置启停 |

### 6.3 过程质量分（category=process，每日 cron 计算）

| 维度 | 数据来源 | 计算方式（默认） |
|------|---------|-----------------|
| 简历处理时效 | Candidate 创建 → 首次阶段推进的时长 | 48h 内处理率 ≥90% 得满分 10，线性递减 |
| 面试反馈催收响应 | InterviewEvaluation.remindedAt → submittedAt | 催收后 24h 内提交率，满分 10 |
| 跟进记录完整度 | CommunicationLog 覆盖率（活跃候选人中有跟进记录的比例） | ≥80% 得满分 10 |
| 人才库维护 | 每周至少 N 次有效操作（标签整理/备注更新/查重处理） | 达标 +10 |

- 过程质量分按**周**为最小结算周期（日粒度噪声太大），由 node-cron 每日凌晨增量计算
- 淡季解释力：业务积分低 + 过程分高 = 没活但态度在线；双低 = 不干活。报表中两个分数并列展示，不合并成一个黑盒数

### 6.4 数据模型（新增 2 表 + 1 字典）

```prisma
model HrScoreEvent {
  id         String   @id @default(cuid())
  userId     String
  ruleCode   String   // 对应 hr_score_rule.code
  category   String   // business / process
  points     Int
  targetType String?  // Candidate / Interview / Offer ...
  targetId   String?
  remark     String?
  bizDate    DateTime @db.Date  // 归属的业务日期（用于周期聚合）
  createdAt  DateTime @default(now())

  @@unique([userId, ruleCode, targetType, targetId])  // 幂等防重复记分
  @@index([userId, bizDate])
  @@map("hr_score_event")
}

model HrScoreSnapshot {
  id           String   @id @default(cuid())
  userId       String
  periodType   String   // day / week / month / quarter / year
  periodStart  DateTime @db.Date
  businessPts  Int
  processPts   Int
  totalScore   Float    // business×70% + process×30%（权重快照到字段，防规则变更回溯）
  rank         Int?     // 同周期排名
  createdAt    DateTime @default(now())

  @@unique([userId, periodType, periodStart])
  @@map("hr_score_snapshot")
}
```

- 字典 `hr_score_rule`：`code / name / points / category / enabled`
- 快照由每日 cron 生成（日快照）+ 周/月/季/年由日快照聚合（查询时实时聚合即可，量小）

### 6.5 页面与接口

| 页面/接口 | 说明 |
|----------|------|
| `GET /api/hr-score/my` | 本人积分明细（事件流水分页）+ 各周期总分 + 排名 |
| `GET /api/hr-score/team` | 团队周期排名（hr 可见名次，admin 可见全分明细） |
| `GET /api/hr-score/report` | admin 报表：周期对比、环比、趋势数据（ECharts） |
| `GET /api/hr-score/export` | admin 导出 Excel（xlsx，复用现有导出依赖） |
| 前端 | 新菜单「我的积分」（hr）/「团队考核」（admin）；Dashboard 可加个人积分卡片 |
| 规则配置 | 设置页新增「积分规则」（admin），启停用 + 分值调整 |

### 6.6 关键设计决策（待确认，有默认方案）

| 决策点 | 默认方案 |
|--------|---------|
| 历史数据 | **不回溯**：功能上线日起记分，之前的数据不补（补记会扭曲当期排名） |
| 负分 | Offer 被拒 / 试用期淘汰记负分，默认启用，可在规则字典关闭 |
| 多人协作归属 | 记分给动作执行人（createdById/操作人），不做拆分 |
| 排名可见性 | hr 看名次 + 自己的分，不显示他人具体分数（避免攀比争议）；admin 看全部 |
| 防刷 | 唯一约束幂等；负分约束质量；admin 可撤销异常事件（写 OperationLog） |

### 6.7 验收标准

- 每个业务动作触发对应积分事件，重复操作不重复记分（幂等）
- 日/周/月/季/年五个周期分数正确聚合，与明细加总一致
- 过程质量分每日 cron 正常计算，单类失败不影响其他
- 排名正确；hr 越权看他人明细返回 403
- 规则字典调整分值后，新事件按新分值记分（旧事件不变）
- 报表导出 Excel 数字与页面一致
- service 单测（重点：聚合逻辑、幂等、边界日期）+ 集成测试

---

## 7. 非功能需求

1. **LLM 可靠性**：所有 AI 功能失败不阻塞主流程；60s 超时（现有）；统一 `callLLM` 入口，`purpose` 标签区分（match-score / jd-polish / jd-draft / interview-outline）
2. **成本**：打分 hash 去重；JD 建议类接口 rate limit；面试大纲限版本数；管理后台可见各 purpose 调用量（Prometheus 指标已有埋点基础）
3. **审计**：AI 调用、打分结果、大纲生成、积分事件、规则变更全部写 OperationLog
4. **权限**：继承候选人可见性与角色权限矩阵；新增权限点 `ai:match-score`、`ai:jd-assist`、`ai:interview-outline`、`hr-score:view-team`
5. **个保法合规**：简历内容发送给第三方 LLM，候选人授权同意文案需覆盖"AI 处理"用途（检查现有 consentNote 文案，必要时更新）
6. **测试**：按仓库规范——service 层单测（mock LLM）、接口集成测试；覆盖率门槛不降

---

## 8. 开发计划

> 沿用本项目的 vibe-coding 惯例：每个功能 = 设计稿文档 → server 实现 → client 实现 → 测试 → 总结文档，按 commit 粒度推进。

| 里程碑 | 内容 | 估时 | 交付物 |
|--------|------|------|--------|
| M1：F2 简历打分 | 字典 + AiMatchScore 表 + BullMQ 任务 + 3 接口 + 详情页/职位列表前端 + 测试 | 6 天 | 可上传自动打分、详情页看分 |
| M2：F1 JD 完善/生成 | 2 接口 + 职位表单两个 AI 弹窗 + 对照确认交互 + 测试 | 4 天 | JD 可 AI 完善、可 AI 起草 |
| M3：F3 面试问题生成 | Interview.focusType 字段 + 考察方向字典 + Outline 表 + 版本管理 + 3 接口 + 面试详情页 + 评估页对照 + 测试 | 5.5 天 | 一键生成（按考察方向差异化出题）+ 指令调整 + 版本回退 |
| M4：F4 HR 考核 | 规则字典 + 2 表 + 事件埋点（散落各 service）+ 每日 cron + 4 接口 + 2 页面 + 报表导出 + 测试 | 8 天 | 积分全链路 + 团队报表 |
| M5：收尾 | 合规文案检查、prompt 调优、文档、生产部署方案更新 | 2 天 | 部署上线 |

**总计约 25.5 个工作日**（单人全栈口径，含测试；AI 联调 buffer 已含）。

**依赖与排期说明**：
- M1 最先：它是 M3 的输入之一，且能最快产出业务价值
- M4 最后：规则复杂、埋点分散在 candidate/interview/offer 多个 service，前面功能稳定后再动
- 每个 M 结束即可部署上线，不必等全部完成

**数据库变更**：4 个迁移（ai_match_score 表 + matching_dimension 字典数据、interview.focusType 字段 + interview_focus_type 字典数据 + interview_question_outline 表、hr_score_event + hr_score_snapshot 表 + hr_score_rule 字典数据），全部纯新增/可空新增、向后兼容，部署风险低。

**环境变量**：无需新增（复用 `LLM_PROVIDER` 等现有配置）。

---

## 9. 风险与开放问题

| 风险 | 缓解 |
|------|------|
| LLM 输出不稳定（分数漂移） | prompt 版本化管理；服务端重算综合分；异常输出重试 1 次后落"打分失败" |
| token 成本失控 | hash 去重 + rate limit + 版本上限；每周看 Prometheus 用量 |
| 积分规则争议 | 规则全字典化可调整；负分可关闭；上线初用默认值试运行 2 周再校准 |
| 过程质量分口径不准 | 先上线观察数据分布，阈值参数放字典可调 |
| 简历 PII 上送 LLM 的合规 | 已确认允许公有云；建议 consentNote 授权文案补充"AI 分析"用途（M5 检查） |

**开放问题**（开发中如遇再定）：
- 打分维度是否需要按职位类型（社招/校招/实习）差异化？v1 先全局一套，字典机制已预留扩展空间
- 面试大纲是否需要英文题目支持？默认不需要

---

*本文档基于 2026-09-01 的 9 轮需求澄清生成；关键决策均已确认，「待确认」项均有默认方案，如无异议按默认执行。*
