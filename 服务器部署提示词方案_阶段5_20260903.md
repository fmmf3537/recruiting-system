# 服务器部署提示词方案（阶段 5：AI 招聘增强 + HR 考核，2026-09-03）

> **用途**：在服务器上用 Kimi Code（或 opencode / dsh）执行招聘管理系统（ATS）阶段 5 生产部署。
> **前置**：上次部署（2026-09-01）已把 master 推到 `a3a50dd` 并经 ⑦ 冒烟通过。本次基于 `a3a50dd` 之后的新提交。
> **使用方法**：把每个提示词代码块**逐条完整粘贴**给服务器上的 AI 编码工具，拿到输出并对照「✅ 检查点」确认后，再贴下一条。
> **总原则**：任何一步不符合检查点 → 立即停止 → 把输出拿回来分析。

---

## 使用前必读（人工确认，不粘贴）

1. **先 push 本地代码**：本地 master 须领先 origin/master 0 个 commit（当前 HEAD `dcf55e6`）。在本地执行 `git push origin master`，否则服务器拉不到阶段 5 的全部代码。（已确认 ✅）
2. **⚠️ 上次部署后的服务器本地改动检查（重要）**：
   - 上次部署（2026-09-01）后，服务器上可能直接手动改过代码（如修简历解析）。**必须先跑提示词 ⓪-B 工作树脏检查**，确认 `git status` 干净或有可控改动后，才能进 ①。
   - 已核实 `fb8b0c1 fix: compose 透传 LLM 简历解析环境变量` **已在 origin/master 上**（通过 git 走的正常修复，不是裸改），且在上次部署版本 `a3a50dd` 之前已含。若服务器实际遇到的"简历解析无法使用"是**另一次未提交的手改**，则此检查会把脏文件暴露出来。
   - **规则**：`git status` 出现未跟踪/修改文件时，**绝不直接 pull**——先按 ⓪-B 处理完再继续。
3. **本次部署实际变更量**：
   - 服务器上次部署停在 `a3a50dd`（2026-09-01 ⑧ 收尾处记录）。本次到 `dcf55e6` 约 **20+ 个 commit**。
   - **待应用数据库迁移 = 4 个**（上次已应用角色 3 个：`20260831010000_add_user_role_hiring_manager_interviewer` / `20260901000000_add_user_role_hr` / `20260901000001_rename_member_to_hr`，本次不需要再跑）：
     1. `20260901000000_add_ai_match_score`（F2 简历打分）
     2. `20260902000000_add_interview_question_outline`（F3 面试大纲）
     3. `20260902130000_add_agency_referral`（F5 猎头通道）
     4. `20260902150000_add_hr_score`（F4 HR 考核）
   - **无 UploadRecord 回填需求**（阶段 5 不走绕过上传的新链路；猎头上传复用 validateAndRenameUpload 会正常写 UploadRecord）
   - **新增环境变量（可选配）**：`HR_SCORE_CRON` / `HR_SCORE_BUSINESS_WEIGHT` / `HR_SCORE_PROCESS_WEIGHT` / `HR_SCORE_TALENT_OPS_WEEKLY`（默认关闭 cron，配了才启用）
4. **准备好**：生产管理员账号密码（冒烟测试登录用）。
5. **全程预计 20\~40 分钟**，备份和迁移是关键路径。

---

## 提示词 ⓪ 全局安全规则（第一段粘贴，约束整个会话）

```markdown
# 角色与安全规则（本次会话全程有效）

你是生产服务器上的部署执行助手。接下来我会分阶段给你部署任务。全程必须遵守：

1. 【禁止破坏性命令】不得执行任何 DROP / TRUNCATE / DELETE FROM / rm -rf / git clean /
   docker system prune / docker volume rm 等删除类操作；不得给任何命令加 --force / -f 强制标志。
2. 【禁止改动三类文件】.env、docker-compose.override.yml、server/uploads/ 绝对不许
   修改、移动、覆盖、删除，git 操作也不得检出覆盖它们。
3. 【写操作白名单】只允许：数据库备份文件写入 backups/、git pull、docker build / tag /
   up -d server、prisma migrate deploy、前端构建写入 client/dist。
   除此之外的任何写操作先问我。
4. 【失败即停】任何命令报错或输出与预期不符：立即停止，原样报告完整错误，
   不得自行重试超过 2 次，不得用 || true 掩盖错误。
5. 【原样汇报】每个检查点命令的输出原样贴给我，不要总结、不要省略数字。
6. 【迁移只跑 4 个】本次待应用迁移是 ai_match_score / interview_question_outline /
   agency_referral / hr_score 4 个；如果 migrate status 显示其他更多未应用迁移，
   停止报告（说明上次部署没跑完全，需要重新核对）。

确认理解后回复"规则已确认"，然后等我发第 1 个任务。
```

---

## 提示词 ⓪-B 【重要】服务器工作树脏状态检查

> **背景**：上次部署（2026-09-01）后，服务器上可能**直接手动改过代码**（例如修简历解析），这些改动可能**没有 commit 回 master**。本次部署前**必须先确认工作树干净**，否则 `git pull` 可能冲突或覆盖掉修复。

```markdown
# 任务：检查服务器 git 工作树是否为干净状态（只读，禁止写操作）

cd /home/ubuntu/recruiting-system

1. git status --short
   # 输出可能为空（✅ 干净）或一行一行列出改动/新增文件
   # 逐行原样输出，不要省略

2. git stash list
   # 输出可能为空（✅ 无 stash）或列出 <<stash@{0}>>

3. git diff --stat   # 已跟踪文件的改动
   # 空 = 没有已跟踪文件改动

4. git diff --name-only   # 列出改动文件路径
   # 这是关键：列出所有 M / D / A 状态的文件，逐条原样给我

5. git ls-files --others --exclude-standard | head -30
   # 新增未跟踪文件（排除 .env / override / uploads 等按 .gitignore 已忽略的）
   # 这能发现"上次手动加的脚本/配置文件"

6. 检查上次"简历解析修复"是否已提交进仓库（对比本次要拉的代码）：
   git log --oneline origin/master -3
   # 如果上次手改过 resume-parser.worker.ts / llm.ts / docker-compose.yml 且没 commit，
   # 这里会看不到那次修复；请把步骤 4 的输出和我给的"阶段 5 变更清单"对比

7. 如果我（人工）给了你阶段 5 变更文件清单，把步骤 4/5 的输出
   与清单中以下文件比对，报告是否重叠：
   - server/src/workers/resume-parser.worker.ts
   - server/src/lib/llm.ts
   - server/src/services/resume-parser.service.ts
   - server/src/services/match-score.service.ts
   - server/src/services/jd-assist.service.ts
   - server/src/services/interview-outline.service.ts
   - server/src/services/hr-score-process.service.ts
   - server/src/services/referral.service.ts
   - server/src/routes/hr-score.ts
   - server/prisma/schema.prisma
   - server/prisma/migrations/*
   - docker-compose.yml / nginx/nginx.conf
   # 任一个重叠 → 立即停止，把重叠情况原样报告，等我决定怎么处理

全部输出原样给我。
```

✅ **检查点**（三类结果）：
- **干净**：`git status --short` 空 → 直接进 ①
- **有改动但不在阶段 5 清单** → 停下，把改动清单给我，评估是 stash 还是先 commit（**绝不直接 pull**）
- **有改动且与阶段 5 清单重叠**（尤其 `resume-parser.worker.ts` / `llm.ts`）→ **立即停止**，这是硬冲突，必须先处理本地改动再部署

---

## 提示词 ① 只读环境侦察

```markdown
# 任务：部署前环境侦察（全部只读，禁止任何写操作）

逐条执行并原样输出：

cd /home/ubuntu/recruiting-system

1. pwd && git rev-parse HEAD && git log --oneline -1
   （预期当前版本是 a3a50dd 或其后某个提交；如果是更早版本，停止并告诉我实际值）

2. git status --short
   （预期只有 deploy.sh 权限位变化或为空；出现其他内容停止报告）

3. docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
   （预期有 ats_server / ats_postgres / ats_redis 三个容器在运行）

4. ls -la .env docker-compose.override.yml server/uploads | head -12
   （确认关键文件存在；记录 .env 和 override 的大小与时间戳，部署后要一致）

5. df -h / | tail -1
   （可用空间 < 5G 则停止报告，需先清理）

6. free -h | head -2

7. docker-compose exec -T server npx prisma migrate status 2>&1 | tail -10
   （预期未应用清单含 4 个：20260901000000_add_ai_match_score /
    20260902000000_add_interview_question_outline /
    20260902130000_add_agency_referral / 20260902150000_add_hr_score；
    已应用的应包含角色 3 个（add_user_role_hr / rename_member_to_hr 等）；
    若显示更多未应用说明上次没迁库，把实际列表原样给我）

8. docker exec ats_postgres psql -U $(grep '^DB_USER=' .env | cut -d= -f2) -d ats_db -c \
   "SELECT (SELECT count(*) FROM candidate) c, (SELECT count(*) FROM job) j, \
            (SELECT count(*) FROM offer) o, (SELECT count(*) FROM interview) i, \
            (SELECT count(*) FROM \"user\") u;"

9. curl -s http://localhost:3001/api/health | head -c 200
   （确认旧服务当前健康，作为部署前基线）

全部输出原样给我，不要做任何修改。
```

✅ **检查点**：版本 a3a50dd 或其后；三容器运行中；.env / override / uploads 就位；磁盘 ≥ 5G；未应用迁移清单为 **4 个**（更多则需先确认）；旧服务健康。

---

## 提示词 ② 备份——不可跳过

```markdown
# 任务：部署前完整备份

cd /home/ubuntu/recruiting-system

## 1. 数据库备份（custom 格式）
mkdir -p backups
TS=$(date +%Y%m%d_%H%M%S)
docker exec ats_postgres pg_dump -U $(grep '^DB_USER=' .env | cut -d= -f2) -Fc ats_db > backups/pre_deploy_$TS.dump

## 2. 验证备份可读（必须做！）
docker cp backups/pre_deploy_$TS.dump ats_postgres:/tmp/check.dump
docker exec ats_postgres pg_restore --list /tmp/check.dump | head -15
docker exec ats_postgres rm /tmp/check.dump
# 能列出表清单 = 备份有效；报错则重新备份，不得继续

## 3. 记录迁移前行数（迁移后对比用）
docker exec ats_postgres psql -U $(grep '^DB_USER=' .env | cut -d= -f2) -d ats_db -c \
"SELECT (SELECT count(*) FROM candidate) c, (SELECT count(*) FROM job) j, (SELECT count(*) FROM offer) o, (SELECT count(*) FROM interview) i, (SELECT count(*) FROM \"user\") u;"

## 4. 文件与配置备份（override 未入 git，必须备份！）
tar czf backups/uploads_$TS.tar.gz server/uploads
cp .env backups/env_$TS
cp docker-compose.override.yml backups/compose-override_$TS.yml
cp docker-compose.yml backups/compose_$TS.yml

## 5. 镜像备份标签 + 回滚锚点
docker tag recruiting-system-server:latest recruiting-system-server:backup-$TS
git rev-parse HEAD > backups/rollback_commit.txt

ls -lh backups/ | tail -8
echo $TS > backups/last_backup_ts.txt
cat backups/rollback_commit.txt
把全部输出原样给我。
```

✅ **检查点**：dump 可列出表清单；5 类备份文件齐全；镜像 backup 标签存在；rollback\_commit.txt 内容为 a3a50dd 开头。**备份未验证通过，绝对禁止进入下一阶段。**

---

## 提示词 ③ 拉取新代码

```markdown
# 任务：拉取新版本代码

cd /home/ubuntu/recruiting-system

1. git fetch origin
2. git stash list   # 确认无 stash 遗留
3. git pull origin master
   （预期快进合并；若报冲突或需要 merge commit，立即停止报告，不得自行解决）
4. git log --oneline -3
   （HEAD 应为包含 003c2f7 的最新提交——003c2f7 是 AGENTS.md 更新，之上的提交属正常）
   git merge-base --is-ancestor a3a50dd HEAD && echo "OK: 上次部署版本在祖先" || echo "FAIL: 版本异常"
   （输出 FAIL 则停止报告）
5. ls server/prisma/migrations/ | grep -E "add_ai_match_score|add_interview_question_outline|add_agency_referral|add_hr_score"
   仓库里最新的迁移文件应到：
   20260901000000_add_ai_match_score
   20260902000000_add_interview_question_outline
   20260902130000_add_agency_referral
   20260902150000_add_hr_score
   （本次待应用的就是这 4 个；角色枚举 2 个（add_user_role_hr / rename_member_to_hr）上次已应用）
6. ls server/src/services/match-score.service.ts server/src/services/hr-score-process.service.ts \
       server/src/routes/hr-score.ts server/src/routes/referral.ts
   （阶段 5 核心服务文件必须存在）
7. ls -la .env docker-compose.override.yml
   （与侦察阶段记录的大小、时间戳完全一致；变了立即停止报告）
8. git status --short（应只剩原本的未跟踪文件）
输出原样给我。
```

✅ **检查点**：a3a50dd 是 HEAD 的祖先；4 个新迁移文件在；.env / override 未被 git 触碰。

---

## 提示词 ④ 构建

```markdown
# 任务：构建后端镜像与前端产物（不重启任何服务）

cd /home/ubuntu/recruiting-system

## 1. 构建后端镜像（不带 -f 参数，compose 自动包含 override）
docker-compose build server
docker images | grep recruiting-system-server
# 预期：新 latest + 旧 backup 标签都在；backup 标签丢了立即停止报告

## 2. 构建前端产物到 client/dist（宿主机 nginx 直接服务此目录）
docker run --rm -v $(pwd)/client:/app -w /app node:20-alpine \
  sh -c "npm config set registry https://registry.npmmirror.com && npm install --no-audit --no-fund && npm run build"
ls -la client/dist/index.html   # 时间戳必须是刚才

⚠️ 任一构建失败：停止，把错误末尾 50 行原样给我，不要重试超过 2 次。
⚠️ 构建期间旧服务还在跑，不影响线上，不用着急。
```

✅ **检查点**：新镜像生成、backup 标签还在、index.html 时间戳为刚才。

---

## 提示词 ⑤ 数据库迁移——风险最高

```markdown
# 任务：执行数据库迁移（备份已验证有效，待应用 4 个）

cd /home/ubuntu/recruiting-system

## 1. 迁移前状态
docker-compose exec -T server npx prisma migrate status 2>&1 | tail -8

## 2. 用新镜像跑迁移（run 复用项目网络，能连到 postgres）
docker-compose run --rm server npx prisma migrate deploy
⚠️ 必须看到 "All migrations have been successfully applied"
⚠️ 失败立即停止，完整错误原样给我，禁止继续、禁止 || true

## 3. 结构验证
docker exec ats_postgres psql -U $(grep '^DB_USER=' .env | cut -d= -f2) -d ats_db -c \
"SELECT (SELECT count(*) FROM information_schema.tables WHERE table_name IN
   ('ai_match_score','interview_question_outline','agency','agency_link',
    'hr_score_event','hr_score_snapshot')) new_tables,
        (SELECT count(*) FROM information_schema.columns
         WHERE table_name='interview' AND column_name='focusType') focus_col,
        (SELECT count(*) FROM dictionary WHERE category='matching_dimension') match_dim,
        (SELECT count(*) FROM dictionary WHERE category='interview_focus_type') focus_type,
        (SELECT count(*) FROM dictionary WHERE category='hr_score_rule') hr_rules;"
# 预期：6 / 1 / 5 / 5 / 8（6 张新表 + focusType 列 + 3 个字典分类种子）
   # 注：字典若 count 为 0 属正常——dictionary.service.ts ensureDefaults 在首次调用时自动初始化，
   # 无需手动 INSERT；此处记录基值便于后续确认自动初始化生效

## 4. 业务行数对比（与备份阶段记录的数字逐一对照，必须完全一致）
docker exec ats_postgres psql -U $(grep '^DB_USER=' .env | cut -d= -f2) -d ats_db -c \
"SELECT (SELECT count(*) FROM candidate) c, (SELECT count(*) FROM job) j, (SELECT count(*) FROM offer) o, (SELECT count(*) FROM interview) i, (SELECT count(*) FROM \"user\") u;"

输出原样给我。
```

✅ **检查点**：迁移全成功（4 个）；结构验证 6/1/5/5/8（字典为 0 属正常，ensureDefaults 自动补）；业务行数与迁移前**完全一致**。

---

## 提示词 ⑥ 切换新版本

```markdown
# 任务：替换后端容器（nginx 是宿主机的，不用动；前端 dist 构建完即生效）

cd /home/ubuntu/recruiting-system
1. docker-compose up -d server    # 只重建 server；postgres/redis 保持不动
2. sleep 25 && curl -s http://localhost:3001/api/health
   预期 database/redis/bullmq 全 ok
3. 经宿主机 nginx 验证全链路：curl -s http://localhost/api/health
4. docker-compose logs --tail=40 server   # 确认无 ERROR、无反复重启
5. docker ps --filter name=ats_ --format "table {{.Names}}\t{{.Status}}"
输出原样给我。
```

✅ **检查点**：直连 3001 和经 nginx 80 的 health 都返回 ok；容器无重启循环。

---

## 提示词 ⑦ 冒烟测试（阶段 5 重点：AI 功能 + 公开路由 + 考核）

```markdown
# 任务：生产冒烟测试（只允许登录和 GET 查询，禁止其他写操作；AI 生成类接口只验证 401/403 权限边界，不实际调 LLM）

cd /home/ubuntu/recruiting-system

1. curl -s http://localhost/api/health
2. curl -s http://localhost/ | head -c 200   # 新前端 index.html
3. 登录（用生产管理员真实账号）：
   curl -s -X POST http://localhost/api/auth/login -H "Content-Type: application/json" \
     -d '{"email":"<管理员邮箱>","password":"<密码>"}' | head -c 120
4. 带 token 抽查核心接口（应有数据返回）：
   curl -s -H "Authorization: Bearer <token>" "http://localhost/api/candidates?page=1&pageSize=5" | head -c 300
   curl -s -H "Authorization: Bearer <token>" "http://localhost/api/jobs?page=1&pageSize=5" | head -c 300
   curl -s -H "Authorization: Bearer <token>" "http://localhost/api/hr-score/my?period=week" | head -c 300
   # 预期 success=true（我的积分接口，即使无事件也返回空明细而非 500）
5. 权限边界验证（新权限点 401/403 应生效）：
   curl -s -o /dev/null -w "ai-no-token=%{http_code}\n" \
     "http://localhost/api/hr-score/team?period=week"   # 预期 401（无 token）
   # 有 token 但不是 admin 时（如 hr 账号）访问 /hr-score/report 应 403，需用非 admin 账号试一次
6. 公开猎头路由（无需 token，预期 410 固定文案或 200 落地页信息，看 token 是否有效）：
   curl -s -o /dev/null -w "referral-gone=%{http_code}\n" \
     "http://localhost/api/referral/fake0000000000000000000000000000"
   # 预期 410（无效 token 统一返回 410，不泄露格式细节）
7. curl -s http://localhost:3001/api/metrics | head -3   # Prometheus 指标端点
全部通过后报告"冒烟通过"，附各项输出。
```

✅ **检查点**：登录成功、列表有数据、/hr-score/my 返回 success、权限边界 401/403 生效、公开路由 410。然后你本人在浏览器完整点一遍：登录 → 候选人详情 → AI 打分卡片 → 职位新建 → AI 辅助 → HR 考核页 → 猎头机构页。

---

## 提示词 ⑧ 回滚预案（仅失败时使用）

```markdown
# 任务：回滚

先报告你要执行哪一级、失败现象是什么，再动手。

## A 级：仅代码回滚（首选，新库结构兼容旧代码）
cd /home/ubuntu/recruiting-system
git checkout $(cat backups/rollback_commit.txt)
TS=$(cat backups/last_backup_ts.txt)
docker tag recruiting-system-server:backup-$TS recruiting-system-server:latest
docker-compose up -d server
# 前端回滚：checkout 后 client/dist 还是新构建的，需重新构建旧版前端：
docker run --rm -v $(pwd)/client:/app -w /app node:20-alpine \
  sh -c "npm config set registry https://registry.npmmirror.com && npm install --no-audit --no-fund && npm run build"
sleep 20 && curl -s http://localhost/api/health
# 注意：4 个迁移是纯新增表/列，旧代码不读新表，A 级回滚无需撤销迁移

## B 级：数据库完整恢复（仅迁移破坏数据时，行数对不上才用）
docker-compose stop server
TS=$(cat backups/last_backup_ts.txt)
docker cp backups/pre_deploy_$TS.dump ats_postgres:/tmp/restore.dump
docker exec ats_postgres pg_restore -U $(grep '^DB_USER=' .env | cut -d= -f2) -d ats_db --clean --if-exists /tmp/restore.dump
docker exec ats_postgres rm /tmp/restore.dump
# 恢复后重新核对业务行数，再执行 A 级代码回滚，最后 docker-compose up -d

回滚后重跑提示词 ⑦ 冒烟测试。
```

---

## 全部通过后的收尾（人工）

1. 观察日志 10 分钟：`docker-compose logs -f server`，确认无 ERROR、定时任务正常输出
2. 备份在服务器保留 **7 天以上**再清理
3. **不要在生产执行** `seed-test-users.ts`（那是本地测试账号脚本）
4. 如需启用 HR 考核定时任务，在 .env 加 `HR_SCORE_CRON=0 2 * * *` 后 `docker-compose up -d server`（可选，默认关闭）
5. 部署成功后 1\~2 周重点观察：Sentry 错误聚合、Prometheus 指标、AI 打分/JD/大纲/猎头/考核 5 功能是否正常

---

## 与上次方案（2026-09-01）的差异说明

| 项 | 上次方案 | 本次（阶段 5） |
|----|---------|--------------|
| 服务器基线版本 | dd29341 | **a3a50dd**（上次部署完成版本） |
| 待应用迁移数 | 3 个（角色枚举） | **4 个**（ai_match_score / interview_question_outline / agency_referral / hr_score） |
| 结构验证 | 4/1/0/0/1（RBAC/软删/角色） | **6/1/5/5/8**（新表/列/字典） |
| UploadRecord 回填 | 需要（老简历 404） | **不需要**（上次已回填，阶段 5 无新绕过上传链路） |
| 冒烟测试 | 老简历下载验证 | 增加 **/hr-score/my + 权限边界 401/403 + 公开路由 410** |
| 新增 env | REMINDER_CRON 等 | **HR_SCORE_CRON / 权重 / 人才库周目标**（可选） |

---

*生成时间：2026-09-03；基于本地 master（003c2f7）+ 阶段 5 全部切片完成 + 上次部署经验（2026-09-01 记录）*

## 部署执行记录（2026-09-03，完成后填）

| 阶段 | 结果 | 关键证据 |
|------|------|---------|
| ① 侦察 | ⏳ | （部署后补填） |
| ② 备份 | ⏳ | |
| ③ 拉取 | ⏳ | |
| ④ 构建 | ⏳ | |
| ⑤ 迁移 | ⏳ | |
| ⑥ 切换 | ⏳ | |
| ⑦ 冒烟 | ⏳ | |

**经验沉淀（部署后补填）**
- （部署后根据实际踩坑补充）