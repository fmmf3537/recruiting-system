# 服务器部署提示词（UI 改造 S0~S5，2026-09-14）

> **用途**：ATS 服务器从 `e8f7d9f` 增量升级到 `ceea74a`（当前 master）。
> **前置**：上次部署（9-04 LLM-FIX）在 `e8f7d9f`；本次**纯前端**，无迁移 / 无新 env / 无 nginx 改动 / server 源码零变更。
> **使用方法**：逐条粘贴给服务器上的 AI 编码工具，对照「✅ 检查点」后继续。
> **总原则**：任何一步不符合检查点 → 停止 → 拿回分析。

---

## 使用前必读（人工确认，不粘贴）

1. **先 push 本地**：本地 master 领先 origin 8 个提交，先 `git push origin master`，确认 origin/master = ceea74a 再开始。
2. **本次变更**（e8f7d9f → ceea74a，20 commit）：
   - **UI-S0~S5 + S1-fix1**（全部为 client/src 改动，19 个文件）：Design Token 层 + 界面状态体系、侧边栏职能分组 + 顶栏面包屑（**默认开启**）、候选人列表新布局（**默认关闭**，灰度开关控制）、候选人录入引导 + 简历解析回填 + 手机号失焦查重、仪表盘 KPI 栅格 + 漏斗图修复、职位表单工具栏减负 + 发布前检查栏。
   - E2E 测试基建（e2e/ 目录，**不上生产**）。
   - server 仅 package.json 加 `dev:e2e` / `db:seed:minimal` 两个脚本，**src 零改动**。
   - docs 若干（不涉代码）。
3. **无迁移**（prisma migrations 无新增）；**无新依赖**（client/package.json 未动）；**无新 env**。
4. **部署方式**：nginx 直接挂载 `client/dist`，前端构建完成即生效，**不需要重启/重建任何容器**，server 全程不动、零中断。

---

## 提示词 ⓪ 全局安全规则（第一段粘贴）

```markdown
# 角色与安全规则（全程有效）
1. 【禁止破坏性】DROP/TRUNCATE/DELETE/rm -rf/git clean/docker system prune/volume rm；不加 --force。
2. 【禁改三类】.env、docker-compose.override.yml、server/uploads/ 不许动，git 不得检出覆盖。
3. 【写白名单】backups/ 备份、git pull、client/dist 前端构建。此外先问我。
4. 【本次不动 server】不执行 docker-compose build/up/restart，不碰任何容器。
5. 【失败即停】报错或不符预期立即停，原样报告，不得重试超 2 次，不得 || true。
6. 【原样汇报】输出原样贴出。
7. 【无迁移】migrate status 应显示 up to date（0 pending）；若出现 pending，停下报告。

确认理解后回复"规则已确认"。
```

---

## 提示词 ① 复核（只读）

```markdown
# 任务：部署前复核
cd /home/ubuntu/recruiting-system

1. pwd && git rev-parse HEAD && git log --oneline -1
   （预期 e8f7d9f 开头）
2. git status --short
   （预期是已知的 ?? 杂项：backups/、.env.bak.*、docker-compose.override.yml 等——全无害）
3. docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
   （ats_server / ats_postgres / ats_redis / ats_nginx 在运行）
4. df -h / | tail -1          # ≥5G
5. free -h | head -2          # available < 500M 报告（构建可能 OOM）
6. docker-compose exec -T server npx prisma migrate status 2>&1 | tail -4
   # 预期 up to date（0 pending；本次无迁移）
7. curl -s http://localhost:3001/api/health | head -c 200
8. curl -s -o /dev/null -w "%{http_code}" http://localhost/   # 经 nginx，预期 200
全部原样输出。
```

✅ **检查点**：e8f7d9f；杂项无害；四容器在；磁盘 OK；migrate up to date；health ok；nginx 200。

---

## 提示词 ② 备份（仅前端，精简）

```markdown
# 任务：备份
cd /home/ubuntu/recruiting-system
mkdir -p backups
TS=$(date +%Y%m%d_%H%M%S)
cp -r client/dist backups/dist_pre_ui_$TS
ls backups/dist_pre_ui_$TS/index.html
git rev-parse HEAD > backups/rollback_ui.txt
echo $TS > backups/last_ui_ts.txt
cat backups/rollback_ui.txt
```

✅ **检查点**：dist 备份完整（含 index.html）；rollback_ui.txt = e8f7d9f 开头。

---

## 提示词 ③ 拉取

```markdown
# 任务：拉取最新 master
cd /home/ubuntu/recruiting-system
1. git fetch origin && git pull origin master
   （预期快进 e8f7d9f → ceea74a；冲突立即停）
2. git log --oneline -3           # 含 ceea74a
3. ls client/src/assets/tokens/ client/src/components/layout/ 2>/dev/null; ls client/src/views/dashboard/ | head -5
   # UI-S0 Design Token 目录在（tokens/ 或类似新文件）
4. ls -la .env docker-compose.override.yml   # 与复核一致
5. git status --short               # 只剩 ?? 杂项
```

✅ **检查点**：ceea74a 在 HEAD；受保护文件未动。

---

## 提示词 ④ 构建（仅前端）

```markdown
# 任务：构建 client
cd /home/ubuntu/recruiting-system

0. free -h | head -2
   # available < 500M → 停下报告（不擅自停其他容器）

1. docker run --rm -v $(pwd)/client:/app -w /app node:20-alpine \
   sh -c "npm config set registry https://registry.npmmirror.com && npm install --no-audit --no-fund && npm run build"

2. ls -la client/dist/index.html   # 时间戳为刚才
3. curl -s -o /dev/null -w "%{http_code}" http://localhost/   # 预期 200（dist 即挂即生效）

⚠️ 失败：停，报错误末尾 50 行。不要重启任何容器。
```

✅ **检查点**：dist 时间戳为刚才；nginx 200。

---

## 提示词 ⑤ 冒烟（重点：UI 改造验证）

```markdown
# 任务：冒烟（只读验证 + 人工浏览器确认）
cd /home/ubuntu/recruiting-system

1. curl -s http://localhost/api/health              # 后端未动，应正常
2. curl -s http://localhost/ | head -c 300          # 首页 HTML 正常
3. 登录拿 token：
curl -s -X POST http://localhost/api/auth/login -H "Content-Type: application/json" \
  -d '{"email":"<管理邮箱>","password":"<密码>"}' | head -c 120

4. ⚠️ 浏览器人工确认（UI 改造核心）：
   a. 登录后侧边栏：应按职能分组显示且带图标，顶栏有面包屑（UI-S1，默认开启）
   b. 折叠侧边栏：折叠态不应出现空白（S1-fix1）
   c. 仪表盘：KPI 卡片栅格对齐、漏斗图标签不重叠（UI-S4）
   d. 职位表单：工具栏精简、有「发布前检查」栏、AI 入口收敛（UI-S5）
   e. 候选人列表：默认仍是旧布局（UI-S2 灰度开关默认关闭，属预期）
   f. 候选人录入：有引导步骤、上传简历可解析回填、手机号失焦查重（UI-S3）
5. 各页面控制台无大面积红色报错（个别 404 静态缓存强刷即可）

全部通过报告"冒烟通过"，附各项输出。
```

✅ **检查点**：侧边栏分组+面包屑生效；折叠不空白；仪表盘/职位表单/录入引导正常；候选人列表默认旧布局（预期）；后端接口不受影响。

---

## 提示词 ⑥ 回滚预案（仅失败时）

```markdown
# 任务：回滚（纯前端，30 秒级）
先报告失败现象，再动手。
TS=$(cat backups/last_ui_ts.txt)
rm -rf client/dist
cp -r backups/dist_pre_ui_$TS client/dist
curl -s -o /dev/null -w "%{http_code}" http://localhost/   # 预期 200
# 无迁移、server 未动，恢复 dist 即完整回滚
```

---

## 部署后收尾（人工）

1. 强刷浏览器（Ctrl+F5）确认新 UI，避免旧 chunk 缓存造成样式错乱。
2. 如需灰度体验候选人列表新布局：在界面设置中打开 UI-S2 开关（默认关闭属预期，非故障）。
3. 观察 10 分钟： nginx access log 无异常 5xx。

---

## 部署执行记录（完成后填）

| 阶段 | 结果 | 证据 |
|------|------|------|
| ① 复核 | ⏳ | |
| ② 备份 | ⏳ | |
| ③ 拉取 | ⏳ | |
| ④ 构建 | ⏳ | |
| ⑤ 冒烟（侧边栏/仪表盘/表单/录入）| ⏳ | |
