# 服务器部署提示词方案（增量：DICT + INTV + AI-SETTINGS，2026-09-04）

> **用途**：ATS 服务器从 `146f519` 增量升级到 `be33c9f`（当前 master）。
> **前置**：上次侦察确认——服务器在 `146f519`、迁移 34 个全应用（相对该版本 up to date）、三容器健康、业务数据在（128 候选人/28 职位/5 Offer/1 面试/7 用户）。
> **使用方法**：逐条粘贴下列提示词给服务器上的 AI 编码工具，对照「✅ 检查点」后继续。
> **总原则**：任何一步不符合检查点 → 停止 → 拿回分析。

---

## 使用前必读（人工确认，不粘贴）

1. **本次增量范围**（146f519 → be33c9f，16 commit）：
   - **DICT** 字典导入导出：server 新增 `xlsx` 依赖；`POST /api/dictionaries/import` + `GET /api/dictionaries/categories`；前端导出/导入弹窗/分类动态化。
   - **INTV** 面试流程修复：权限修正（complete/cancel/工作台开放 hiring_manager）+ `GET /api/users/interviewer-options` + 前端入口恢复/内嵌弹窗/一键二连/大纲生成 + 列表 evaluations/round。
   - **AI-SETTINGS** AI 提供方/密钥界面化管理：AES-256-GCM 密文落库、保存热生效。
2. **⚠️ 数据库迁移 = 1 个（重要，上次 C 判断有误已修正）**：
   - `20260903160000_add_ai_provider_config`（AI 密钥表，纯新增表，向后兼容）
   - 服务器 146f519 目录无此迁移 → pull 后 migrate status 会显示 **1 个 pending**（不是 0）
3. **无 UploadRecord 回填、无新增 env**（AI-SETTINGS 用现有 .env，密钥存在 DB 里）。
4. **唯一依赖变化**：server 镜像新增 `xlsx@^0.18.5`（构建变重 ~10MB）。
5. **服务器多项目共存**：同一台机跑着 ATS + fitness-hub + CRM 共 8 容器；内存仅 3.6G 且可用 ~1G——**构建时若 OOM，先临时停非 ATS 容器（fitness-hub-frontend/backend/backup）再构建**，构建完重启。
6. 周期约 15~30 分钟。

---

## 提示词 ⓪ 全局安全规则（第一段粘贴）

```markdown
# 角色与安全规则（本次会话全程有效）

你是生产服务器上的部署执行助手。全程必须遵守：
1. 【禁止破坏性命令】不得 DROP / TRUNCATE / DELETE / rm -rf / git clean / docker system prune / docker volume rm；
   不得加 --force / -f。
2. 【禁止改动三类文件】.env、docker-compose.override.yml、server/uploads/ 绝对不许改/移/覆盖/删，
   git 操作不得检出覆盖它们。
3. 【写操作白名单】只允许：backups/ 写备份、git pull、docker build / tag / up -d server、
   prisma migrate deploy、前端构建写 client/dist。此外先问我。
4. 【失败即停】命令报错或输出不符预期：立即停止，原样报告，不得重试超 2 次，不得 || true 掩盖。
5. 【原样汇报】检查点命令输出原样贴出，不总结不省略数字。
6. 【迁移只有 1 个】pull 后待应用仅 `20260903160000_add_ai_provider_config`；
   若出现其他 pending，停止报告。

确认理解后回复"规则已确认"。
```

---

## 提示词 ① 增量前复核（只读）

```markdown
# 任务：部署前复核（只读，禁止写操作）

cd /home/ubuntu/recruiting-system

1. pwd && git rev-parse HEAD && git log --oneline -1
   （预期 146f519562cd4d4e1ce29836dde3ad708ffe0d73，146f519）

2. git status --short
   （预期是上次记录过的一堆 ?? 未跟踪杂项：backups/、.env.bak.*、docker-compose.override.yml、
     docker-compose.yml.bak.*、health-check.sh、client/package-lock.json 等——全部无害。

3. docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
   （ats_server / ats_postgres / ats_redis 在运行；fitness-hub / crm 等其它项目容器不动）

4. df -h / | tail -1     # 预期 ≥5G（上次 11G）
5. free -h | head -2     # 内存：若 available < 500M 报告，构建可能 OOM
6. docker exec ats_postgres psql -U $(grep '^DB_USER=' .env | cut -d= -f2) -d ats_db -c \
   "SELECT (SELECT count(*) FROM candidate) c, (SELECT count(*) FROM job) j, \
           (SELECT count(*) FROM offer) o, (SELECT count(*) FROM interview) i, \
           (SELECT count(*) FROM \"user\") u;"
   （与上次 128/28/5/1/7 对比，应一致——若差异大说明有线上写入，备份时注意）
7. curl -s http://localhost:3001/api/health | head -c 200

全部原样输出。
```

✅ **检查点**：版本 146f519；杂项 ?? 无害；三容器在；磁盘 ≥5G；行数 ≈ 上次；health ok。

---

## 提示词 ② 备份（不可跳过，但可精简）

```markdown
# 任务：部署前备份

cd /home/ubuntu/recruiting-system
mkdir -p backups
TS=$(date +%Y%m%d_%H%M%S)

## 1. 数据库备份（custom 格式）+ 验证可读
docker exec ats_postgres pg_dump -U $(grep '^DB_USER=' .env | cut -d= -f2) -Fc ats_db > backups/pre_delta_$TS.dump
docker cp backups/pre_delta_$TS.dump ats_postgres:/tmp/check.dump
docker exec ats_postgres pg_restore --list /tmp/check.dump | head -15
docker exec ats_postgres rm /tmp/check.dump
# 能列出表清单 = 有效；报错则重新备份，不得继续

## 2. 迁移前行数（迁移后对比）
docker exec ats_postgres psql -U $(grep '^DB_USER=' .env | cut -d= -f2) -d ats_db -c \
"SELECT (SELECT count(*) FROM candidate) c, (SELECT count(*) FROM job) j, (SELECT count(*) FROM offer) o;"

## 3. 镜像是回滚锚点
docker tag recruiting-system-server:latest recruiting-system-server:backup-delta-$TS
git rev-parse HEAD > backups/rollback_delta.txt
echo $TS > backups/last_delta_ts.txt
ls -lh backups/ | tail -6
```

✅ **检查点**：dump 可读；镜像 backup 标签在；rollback_delta.txt = 146f519 开头。

---

## 提示词 ③ 拉取代码

```markdown
# 任务：拉取最新 master

cd /home/ubuntu/recruiting-system
1. git fetch origin
2. git pull origin master
   （预期快进 146f519 → be33c9f；冲突立即停止）
3. git log --oneline -3
   # HEAD 应含 be33c9f；且 merge-base --is-ancestor 146f519 HEAD 应成功
4. ls server/prisma/migrations/ | grep "20260903160000_add_ai_provider_config"
   # 仓库里应有此迁移（pull 后服务器目录也会出现）
5. ls server/src/utils/xlsx-import.ts server/src/routes/users.ts \
      server/src/services/match-score.service.ts 2>/dev/null
   # DICT/INTV 关键文件在
6. ls -la .env docker-compose.override.yml
   # 与复核时一致（时间戳不变）
7. git status --short   # 应只剩 ?? 杂项，无 M 文件
输出原样给我。
```

✅ **检查点**：be33c9f 在 HEAD；ai_provider_config 迁移目录出现；.env / override 未动。

---

## 提示词 ④ 构建（注意内存）

```markdown
# 任务：构建 server 镜像 + 前端产物（不重启）

cd /home/ubuntu/recruiting-system

## 0. 内存预检：free -h | head -2
# available < 500M → 先临时停非 ATS 容器释放内存：
#   docker stop fitness-hub-frontend-1 fitness-hub-backend-1 fitness-hub-backup-1 crm-app-prod
#   （构建完 docker start 恢复）

## 1. 构建后端（含 xlsx 依赖，镜像会变大）
docker-compose build server
docker images | grep recruiting-system-server
# 预期：新 latest + 旧 backup-delta 标签都在

## 2. 构建前端到 client/dist（宿主机 nginx 直接服务）
docker run --rm -v $(pwd)/client:/app -w /app node:20-alpine \
  sh -c "npm config set registry https://registry.npmmirror.com && npm install --no-audit --no-fund && npm run build"
ls -la client/dist/index.html   # 时间戳为刚才

⚠️ 任一构建失败：停止，报错误末尾 50 行。不要重试超 2 次。
```

✅ **检查点**：新镜像 + backup 标签在；dist/index.html 新。

---

## 提示词 ⑤ 数据库迁移（1 个）

```markdown
# 任务：应用 ai_provider_config 迁移

cd /home/ubuntu/recruiting-system
1. docker-compose exec -T server npx prisma migrate status 2>&1 | tail -6
   # 应显示 1 个 pending：20260903160000_add_ai_provider_config
2. docker-compose run --rm server npx prisma migrate deploy
   # 必须看到 "All migrations have been successfully applied"
   # 失败立即停止完整报错
3. 结构验证：
docker exec ats_postgres psql -U $(grep '^DB_USER=' .env | cut -d= -f2) -d ats_db -c \
"SELECT count(*) FROM information_schema.tables WHERE table_name='ai_provider_config';"
# 预期 1
4. 业务行数对比（候选/职位/Offer 与 ② 备份一致，证明纯新增表无副作用）
输出原样给我。
```

✅ **检查点**：1 pending → 全应用；ai_provider_config 表存在；行数不变。

---

## 提示词 ⑥ 切换新版本

```markdown
# 任务：重建 server + 验证

cd /home/ubuntu/recruiting-system
1. docker-compose up -d server
2. sleep 25 && curl -s http://localhost:3001/api/health
   # db/redis/bullmq 全 ok
3. curl -s http://localhost/api/health       # 经宿主机 nginx
4. docker-compose logs --tail=40 server      # 无 ERROR、无重启循环
5. docker ps --filter name=ats_ --format "table {{.Names}}\t{{.Status}}"
```

✅ **检查点**：直连 + nginx health ok；无重启循环。

---

## 提示词 ⑦ 冒烟（增量重点：DICT + INTV + AI-SETTINGS）

```markdown
# 任务：冒烟测试（只读 + 登录验证，不实际调 LLM / 不改数据）

cd /home/ubuntu/recruiting-system

1. curl -s http://localhost/api/health
2. curl -s http://localhost/ | head -c 200     # 新前端
3. 登录拿 token：
curl -s -X POST http://localhost/api/auth/login -H "Content-Type: application/json" \
  -d '{"email":"<管理邮箱>","password":"<密码>"}' | head -c 120
# 记下 token

4. 面试官下拉（INTV 新接口，登录即可 200）：
curl -s -H "Authorization: Bearer <token>" "http://localhost/api/users/interviewer-options" | head -c 300
   # 预期 data 数组含 4 角色用户

5. 词典分类（DICT 新接口）：
curl -s -H "Authorization: Bearer <token>" "http://localhost/api/dictionaries/categories" | head -c 300
   # 预期 string[] 含 department / interview_focus_type 等

6. 面试列表（应带 round 筛选能力 + evaluations）：
curl -s -H "Authorization: Bearer <token>" "http://localhost/api/interviews?round=初试&page=1&pageSize=5" | head -c 400

7. AI 设置接口（AI-SETTINGS，实际挂 /api/settings 下）：
curl -s -H "Authorization: Bearer <token>" "http://localhost/api/settings/ai-providers" | head -c 300
   # 预期 data 含 provider 列表（如 minimax/deepseek 等配置项）

8. 权限边界抽查（hr 安排场景）：
用 hr 账号登录后 curl /api/users 应 403（说明仍 admin-only），/api/users/interviewer-options 应 200

全部通过报告"冒烟通过"，附各项输出。
```

✅ **检查点**：interviewer-options / dictionaries-categories / interviews?round 返回正常；权限边界符合。

---

## 提示词 ⑧ 回滚预案（仅失败时）

```markdown
# 任务：回滚

先报告要执行哪一级、失败现象，再动手。

## A 级：代码回滚（首选，1 迁移是纯新增表，旧代码不读新表）
cd /home/ubuntu/recruiting-system
TS=$(cat backups/last_delta_ts.txt)
git checkout $(cat backups/rollback_delta.txt)
docker tag recruiting-system-server:backup-delta-$TS recruiting-system-server:latest
docker-compose up -d server
docker run --rm -v $(pwd)/client:/app -w /app node:20-alpine \
  sh -c "npm config set registry https://registry.npmmirror.com && npm install --no-audit --no-fund && npm run build"
sleep 20 && curl -s http://localhost/api/health
# ai_provider_config 表保留无害（旧代码不读）

## B 级：数据库恢复（仅迁移破坏数据时，行数对不上才用）
docker-compose stop server
TS=$(cat backups/last_delta_ts.txt)
docker cp backups/pre_delta_$TS.dump ats_postgres:/tmp/restore.dump
docker exec ats_postgres pg_restore -U $(grep '^DB_USER=' .env | cut -d= -f2) -d ats_db --clean --if-exists /tmp/restore.dump
docker exec ats_postgres rm /tmp/restore.dump
# 再执行 A 级代码回滚，最后 docker-compose up -d server

回滚后重跑 ⑦ 冒烟。
```

---

## 全部通过后的收尾（人工）

1. 若 ④ 为了构建停了 fitness-hub / crm 容器 → `docker start` 恢复所有
2. 观察日志 10 分钟：`docker-compose logs -f server`
3. 确认 AI 设置页（/settings/ai）能打开 + 保存密钥（保存即热生效——首次保存会写 ai_provider_config 表）
4. 字典页试试「导出」当前分类（验证 xlsx 前端链路）+「导入」下载模板
5. 面试管理页浏览器过一遍：安排弹窗（HR 下拉有面试官）、工作台（用人经理能进）、评估列
6. 备份保留 7 天以上

---

## 部署执行记录（2026-09-04，完成后填）

| 阶段 | 结果 | 关键证据 |
|------|------|---------|
| ① 复核 | ⏳ | |
| ② 备份 | ⏳ | |
| ③ 拉取 | ⏳ | |
| ④ 构建 | ⏳ | |
| ⑤ 迁移（1 个）| ⏳ | |
| ⑥ 切换 | ⏳ | |
| ⑦ 冒烟 | ⏳ | |

**经验沉淀（完成后填）**
- （实录：构建是否 OOM、AI 设置接口路径、interviewer-options 实测返回等）